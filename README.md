# test
 // --- MUSIKTHEORIE-GEHIRN --- const NOTES = ['C', 'Db', 'D', 'Eb', 'E', 'F', 'Gb', 'G', 'Ab', 'A', 'Bb', 'B']; const MAJOR_INTERVALS = [0, 2, 4, 5, 7, 9, 11, 12]; const SOLFEGE = ['Do', 'Re', 'Mi', 'Fa', 'Sol', 'La', 'Ti', "Do'"]; const getNote = (rootName: string, interval: number, octave: number) => { const rootIndex = NOTES.indexOf(rootName); const totalSemitones = rootIndex + interval; const noteIndex = totalSemitones % 12; const octaveShift = Math.floor(totalSemitones / 12); return ${NOTES[noteIndex]}${octave + octaveShift}; }; export default function App() { const [isPlaying, setIsPlaying] = useState(false); const [currentRoot, setCurrentRoot] = useState("C"); // NEU: Akkorde speichern jetzt auch ihre Dauer (duration) in Beats! const [chordSequence, setChordSequence] = useState<{degree: string, id: string, duration: number}[]>([]); const [melodyNotes, setMelodyNotes] = useState<{id: string, beat: number, degree: number, duration: number}[]>([]); // States für Notenlänge und Instrumente const [selectedDuration, setSelectedDuration] = useState<number>(4); // Standard jetzt 4 (Ganze Note / ganzer Takt) const [melodyInstrument, setMelodyInstrument] = useState<Tone.ToneOscillatorType>("triangle"); const [chordInstrument, setChordInstrument] = useState<Tone.ToneOscillatorType>("sine"); const [showImportModal, setShowImportModal] = useState(false); const [importText, setImportText] = useState(""); const [isImporting, setIsImporting] = useState(false); const [playbackBeat, setPlaybackBeat] = useState(0); const animationRef = useRef<number>(); const [manualChordNotes, setManualChordNotes] = useState<string[]>([]); const [manualMelodyNotes, setManualMelodyNotes] = useState<string[]>([]); const manualChordTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const manualMelodyTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const chordSynthRef = useRef<Tone.PolySynth | null>(null); const melodySynthRef = useRef<Tone.PolySynth | null>(null); // Umgestellt auf PolySynth zur Fehlervermeidung bei überlappenden Noten const diatonicChords = useMemo(() => { return [ { degree: 'I', name: currentRoot, quality: 'M', notes: [getNote(currentRoot, 0, 4), getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4)] }, { degree: 'ii', name: getNote(currentRoot, 2, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 2, 4), getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4)] }, { degree: 'iii', name: getNote(currentRoot, 4, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4)] }, { degree: 'IV', name: getNote(currentRoot, 5, 0), quality: 'M', notes: [getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4)] }, { degree: 'V', name: getNote(currentRoot, 7, 0), quality: 'M', notes: [getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4)] }, { degree: 'vi', name: getNote(currentRoot, 9, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4), getNote(currentRoot, 16, 4)] }, { degree: 'vii°', name: getNote(currentRoot, 11, 0) + 'dim', quality: 'dim', notes: [getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4), getNote(currentRoot, 17, 4)] }, ]; }, [currentRoot]); // NEU: Berechnung der aktiven Akkord-Töne basierend auf variabler Akkordlänge const activeChordNotes = useMemo(() => { if (!isPlaying) return manualChordNotes; const notes = new Set<string>(); let currentBeatAccumulator = 0; for (const chordInfo of chordSequence) { if (playbackBeat >= currentBeatAccumulator && playbackBeat < currentBeatAccumulator + chordInfo.duration) { const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { chordData.notes.forEach(n => notes.add(n)); } break; // Wir haben den aktuell spielenden Akkord gefunden } currentBeatAccumulator += chordInfo.duration; } return Array.from(notes); }, [isPlaying, playbackBeat, chordSequence, diatonicChords, manualChordNotes]); const activeMelodyNotes = useMemo(() => { if (!isPlaying) return manualMelodyNotes; const notes = new Set<string>(); melodyNotes.forEach(note => { if (playbackBeat >= note.beat && playbackBeat < note.beat + note.duration) { const noteInterval = MAJOR_INTERVALS[note.degree - 1]; notes.add(getNote(currentRoot, noteInterval, 5)); } }); return Array.from(notes); }, [isPlaying, playbackBeat, melodyNotes, currentRoot, manualMelodyNotes]); const triggerManualChordNotes = (notes: string[], durationMs = 800) => { setManualChordNotes(notes); if (manualChordTimeoutRef.current) clearTimeout(manualChordTimeoutRef.current); manualChordTimeoutRef.current = setTimeout(() => setManualChordNotes([]), durationMs); }; const triggerManualMelodyNotes = (notes: string[], durationMs = 800) => { setManualMelodyNotes(notes); if (manualMelodyTimeoutRef.current) clearTimeout(manualMelodyTimeoutRef.current); manualMelodyTimeoutRef.current = setTimeout(() => setManualMelodyNotes([]), durationMs); }; const initAudio = async () => { if (Tone.context.state !== 'running') { await Tone.start(); } if (!chordSynthRef.current) { chordSynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); chordSynthRef.current.volume.value = -10; } if (!melodySynthRef.current) { // PolySynth verhindert Abstürze durch überlappende Noten zur selben Zählzeit melodySynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); melodySynthRef.current.volume.value = -6; } chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }; useEffect(() => { if (chordSynthRef.current) chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); if (melodySynthRef.current) melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }, [chordInstrument, melodyInstrument]); // NEU: Konvertierung von Beats in Tone.js Dauer (inklusive Ganzer Note '1m') const getToneDuration = (dur: number) => { if (dur === 0.25) return "16n"; if (dur === 0.5) return "8n"; if (dur === 1) return "4n"; if (dur === 2) return "2n"; if (dur === 4) return "1m"; // 1 ganzer Takt (measure) return "4n"; }; const togglePlayback = async () => { await initAudio(); if (isPlaying) { Tone.Transport.stop(); Tone.Transport.cancel(0); setIsPlaying(false); setPlaybackBeat(0); if (animationRef.current) cancelAnimationFrame(animationRef.current); } else { Tone.Transport.cancel(0); Tone.Transport.bpm.value = 120; Tone.Transport.loop = true; Tone.Transport.loopEnd = "4m"; // NEU: Akkorde anhand ihrer Dauer auf der Timeline platzieren let currentChordBeat = 0; chordSequence.forEach((chordInfo) => { if (currentChordBeat >= 16) return; // Maximal 4 Takte (16 Beats) const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { const measure = Math.floor(currentChordBeat / 4); const beat = Math.floor(currentChordBeat % 4); const sixteenths = Math.round((currentChordBeat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; Tone.Transport.schedule((time) => { chordSynthRef.current?.triggerAttackRelease(chordData.notes, getToneDuration(chordInfo.duration), time); }, startTime); } currentChordBeat += chordInfo.duration; }); // Melodie platzieren melodyNotes.forEach(note => { const measure = Math.floor(note.beat / 4); const beat = Math.floor(note.beat % 4); const sixteenths = Math.round((note.beat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; const noteInterval = MAJOR_INTERVALS[note.degree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); Tone.Transport.schedule((time) => { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(note.duration), time); }, startTime); }); Tone.Transport.start(); setIsPlaying(true); } }; useEffect(() => { const updateCursor = () => { if (isPlaying) { const currentTicks = Tone.Transport.ticks; const currentBeat = currentTicks / Tone.Transport.PPQ; setPlaybackBeat(currentBeat); animationRef.current = requestAnimationFrame(updateCursor); } }; if (isPlaying) { animationRef.current = requestAnimationFrame(updateCursor); } return () => { if (animationRef.current) cancelAnimationFrame(animationRef.current); }; }, [isPlaying]); useEffect(() => { if (isPlaying) togglePlayback(); // eslint-disable-next-line react-hooks/exhaustive-deps }, [currentRoot]); // NEU: Akkord fügt sich mit der aktuell gewählten Länge (selectedDuration) ein const playAndAddChord = async (chordDegree: string) => { await initAudio(); const chord = diatonicChords.find(c => c.degree === chordDegree); if (chord && chordSynthRef.current) { // Akkord mit der gewählten Länge abspielen (mindestens 8tel für gutes Feedback) const previewDuration = selectedDuration < 0.5 ? 0.5 : selectedDuration; chordSynthRef.current.triggerAttackRelease(chord.notes, getToneDuration(previewDuration)); setChordSequence([...chordSequence, { degree: chord.degree, id: Math.random().toString(), duration: selectedDuration }]); triggerManualChordNotes(chord.notes, previewDuration * 500); } }; const removeChord = (id: string) => setChordSequence(chordSequence.filter(c => c.id !== id)); const removeNote = (e: React.MouseEvent, id: string) => { e.stopPropagation(); setMelodyNotes(melodyNotes.filter(n => n.id !== id)); }; const handleGridClick = async (e: React.MouseEvent<HTMLDivElement>) => { const rect = e.currentTarget.getBoundingClientRect(); const clientX = e.clientX; const clientY = e.clientY; await initAudio(); const x = clientX - rect.left; const y = clientY - rect.top; const beatWidth = rect.width / 16; let clickedBeat = x / beatWidth; clickedBeat = Math.floor(clickedBeat / selectedDuration) * selectedDuration; const rowHeight = rect.height / 8; const clickedRowIndex = Math.floor(y / rowHeight); const scaleDegree = 8 - clickedRowIndex; const noteInterval = MAJOR_INTERVALS[scaleDegree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); if (!isPlaying) { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(selectedDuration)); triggerManualMelodyNotes([actualNote], selectedDuration * 500); } setMelodyNotes([...melodyNotes, { id: Math.random().toString(), beat: clickedBeat, degree: scaleDegree, duration: selectedDuration }]); }; const normalizeChordName = (rawChord: string) => { let clean = rawChord.replace(':maj', '').replace(':min', 'm'); clean = clean.charAt(0).toUpperCase() + clean.slice(1); return clean; }; const mapChordsToDegrees = (chordNamesArray: string[]) => { return chordNamesArray.map(chordName => { const match = diatonicChords.find(c => c.name.toLowerCase() === chordName.toLowerCase()); if (match) { // Standardmässig 4 Beats (1 ganzer Takt) pro importiertem Akkord return { degree: match.degree, id: Math.random().toString(), duration: 4 }; } return null; }).filter(Boolean) as {degree: string, id: string, duration: number}[]; }; const handleImport = async () => { const text = importText.trim(); if (!text) return; try { setIsImporting(true); if (text.startsWith('http')) { let html = ""; try { const proxyUrl = https://api.codetabs.com/v1/proxy?quest=${encodeURIComponent(text)}; const response = await fetch(proxyUrl); if (!response.ok) throw new Error("Proxy 1 failed"); html = await response.text(); } catch (err) { const proxyUrl2 = https://api.allorigins.win/raw?url=${encodeURIComponent(text)}; const response2 = await fetch(proxyUrl2); if (!response2.ok) throw new Error("Proxy 2 failed"); html = await response2.text(); } const chordRegex = /data-chord="([^"]+)"/g; let matches; const foundChords: string[] = []; while ((matches = chordRegex.exec(html)) !== null) { if (matches[1] !== "N") { foundChords.push(normalizeChordName(matches[1])); } } if (foundChords.length === 0) { alert("Konnte keine Akkorde auf dieser Seite finden. Bitte überprüfe die URL."); setIsImporting(false); return; } const uniqueProgression: string[] = []; for (const chord of foundChords) { if (uniqueProgression.length === 0 || uniqueProgression[uniqueProgression.length - 1] !== chord) { uniqueProgression.push(chord); } } const newChords = mapChordsToDegrees(uniqueProgression); if (newChords.length > 0) { setChordSequence(newChords); } else { alert(Akkorde gefunden, aber sie passen nicht zur aktuellen Tonart ${currentRoot}-Dur! Wechsle die Tonart und versuche es nochmals.); } } else if (text.startsWith('{')) { const data = JSON.parse(text); if (data.chords && Array.isArray(data.chords)) { const newChords = data.chords.map((c: any) => { // Unterstützt Strings oder Objekte mit duration if (typeof c === 'string') return { degree: c, id: Math.random().toString(), duration: 4 }; return { degree: c.degree, id: Math.random().toString(), duration: c.duration || 4 }; }); setChordSequence(newChords); } if (data.melody && Array.isArray(data.melody)) { const newMelody = data.melody.map((m: any) => ({ id: Math.random().toString(), beat: m.beat || 0, degree: m.degree || 1, duration: m.duration || 1 })); setMelodyNotes(newMelody); } } else { const chordNames = text.replace(/,/g, ' ').split(/\s+/).filter(Boolean).map(normalizeChordName); const newChords = mapChordsToDegrees(chordNames); if (newChords.length > 0) { setChordSequence(newChords); } } setShowImportModal(false); setImportText(""); } catch (error) { console.error(error); alert("Fehler beim Verarbeiten. Bitte überprüfe den Link oder das Format."); } finally { setIsImporting(false); } }; const displayMeasure = Math.floor(playbackBeat / 4) + 1; const displayBeat = Math.floor(playbackBeat % 4) + 1; const PianoWhiteKey = ({ note, hasBlack, blackNote }: { note: string, hasBlack: boolean, blackNote?: string }) => { const isWhiteMelody = activeMelodyNotes.includes(note); const isWhiteChord = activeChordNotes.includes(note); const isBlackMelody = blackNote ? activeMelodyNotes.includes(blackNote) : false; const isBlackChord = blackNote ? activeChordNotes.includes(blackNote) : false; let whiteBg = 'bg-white hover:bg-slate-50'; if (isWhiteMelody) whiteBg = 'bg-red-400 border-red-500 shadow-[inset_0_0_10px_rgba(239,68,68,0.5)] text-white'; else if (isWhiteChord) whiteBg = 'bg-blue-300 border-blue-400 shadow-[inset_0_0_10px_rgba(59,130,246,0.5)]'; let blackBg = 'bg-slate-900 shadow-[0_2px_2px_rgba(0,0,0,0.5)]'; if (isBlackMelody) blackBg = 'bg-red-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; else if (isBlackChord) blackBg = 'bg-blue-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; return ( <div className="relative flex-shrink-0 z-0"> <div className={w-8 h-24 border border-slate-300 rounded-b-md transition-colors duration-75 flex items-end justify-center pb-1 ${whiteBg}}> {note.startsWith('C') && <span className={text-[9px] font-bold ${isWhiteMelody ? 'text-red-100' : 'text-slate-400'}}>{note}</span>} </div> {hasBlack && ( <div className={absolute -right-3 top-0 w-6 h-14 rounded-b-sm z-10 transition-colors duration-75 ${blackBg}} /> )} </div> ); }; return ( <div className="h-screen w-full flex flex-col bg-slate-50 text-slate-800 font-sans select-none relative"> {/* IMPORT MODAL */} {showImportModal && ( <div className="absolute inset-0 bg-slate-900/40 backdrop-blur-sm z-50 flex items-center justify-center p-4"> <div className="bg-white rounded-xl shadow-2xl w-full max-w-lg overflow-hidden flex flex-col"> <div className="flex items-center justify-between p-4 border-b bg-slate-50"> <h2 className="font-bold text-lg text-slate-700">Song importieren</h2> <button onClick={() => setShowImportModal(false)} className="text-slate-400 hover:text-slate-600 disabled:opacity-50" disabled={isImporting}> <X size={20} /> </button> </div> <div className="p-6 flex flex-col gap-4"> <div> <label className="block text-sm font-bold text-slate-600 mb-1"> Chordify-Link, Text oder JSON </label> <textarea className="w-full h-32 border rounded-lg p-3 text-sm font-mono focus:ring-2 focus:ring-blue-500 focus:outline-none disabled:bg-slate-100 disabled:text-slate-400" placeholder="Füge hier eine URL ein (z.B. https://chordify.net/...)&#10;Oder tippe Akkorde: C G Am F" value={importText} onChange={(e) => setImportText(e.target.value)} disabled={isImporting} /> </div> </div> <div className="p-4 border-t bg-slate-50 flex justify-end gap-2"> <button onClick={() => setShowImportModal(false)} disabled={isImporting} className="px-4 py-2 rounded-lg font-semibold text-slate-600 hover:bg-slate-200 transition-colors disabled:opacity-50" > Abbrechen </button> <button onClick={handleImport} disabled={isImporting || !importText.trim()} className="flex items-center gap-2 px-4 py-2 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition-colors shadow-sm disabled:opacity-70" > {isImporting ? <><Loader2 size={16} className="animate-spin" />Analysiere...</> : "Importieren"} </button> </div> </div> </div> )} {/* 1. HEADER */} <header className="h-14 bg-white border-b flex items-center px-4 justify-between shadow-sm shrink-0 z-10"> <div className="flex items-center gap-6"> <h1 className="font-bold text-xl text-blue-600 tracking-tight">TheoryPad</h1> <div className="flex items-center gap-2 border-l pl-6"> <button onClick={togglePlayback} className={w-10 h-10 rounded-full flex items-center justify-center transition-colors shadow-sm text-white ${isPlaying ? 'bg-red-500 hover:bg-red-600' : 'bg-blue-500 hover:bg-blue-600'}} > {isPlaying ? <Square size={16} fill="currentColor" /> : <Play size={18} fill="currentColor" className="ml-1" />} </button> <div className="flex flex-col ml-2 min-w-[60px]"> <span className="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Position</span> <span className="text-sm font-semibold tabular-nums leading-none">Takt {displayMeasure}.{displayBeat}</span> </div> </div> </div> <div className="flex items-center gap-4"> <div className="flex bg-slate-100 rounded-md p-1 shadow-inner items-center"> <select className="bg-transparent px-2 py-1 font-semibold text-sm cursor-pointer hover:bg-slate-200 rounded outline-none appearance-none" value={currentRoot} onChange={(e) => setCurrentRoot(e.target.value)} > {NOTES.map(note => ( <option key={note} value={note}>{note} Major</option> ))} </select> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">120 BPM</div> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">4/4</div> </div> <button onClick={() => setShowImportModal(true)} className="flex items-center gap-1.5 px-3 py-1.5 bg-slate-800 text-white text-sm font-bold rounded-md hover:bg-slate-700 transition-colors shadow-sm" > <Download size={16} /> Import </button> </div> </header> {/* 1.5 TOOLBAR (Längen & Instrumente) */} <div className="h-12 bg-white border-b flex items-center px-4 justify-between shrink-0 text-sm z-10 shadow-sm"> <div className="flex items-center gap-2"> <Clock size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500 mr-2">Notenwert:</span> <div className="flex bg-slate-100 rounded-lg p-1"> {[ { label: '1/16', val: 0.25 }, { label: '1/8', val: 0.5 }, { label: '1/4', val: 1 }, { label: '1/2', val: 2 }, { label: '1/1 (Ganze)', val: 4 }, ].map(dur => ( <button key={dur.label} onClick={() => setSelectedDuration(dur.val)} className={px-3 py-1 rounded-md font-bold text-xs transition-colors ${selectedDuration === dur.val ? 'bg-white shadow-sm text-blue-600' : 'text-slate-500 hover:bg-slate-200'}} > {dur.label} </button> ))} </div> </div> <div className="flex items-center gap-6"> <div className="flex items-center gap-2"> <Music size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Melodie:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={melodyInstrument} onChange={(e) => setMelodyInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="triangle">Flöte (Triangle)</option> <option value="square">8-Bit (Square)</option> <option value="sawtooth">Synth Lead (Saw)</option> <option value="sine">Glocke (Sine)</option> </select> </div> <div className="flex items-center gap-2 border-l pl-6"> <Volume2 size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Akkorde:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={chordInstrument} onChange={(e) => setChordInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="sine">E-Piano (Sine)</option> <option value="triangle">Warm Pad (Triangle)</option> <option value="sawtooth">Trance (Saw)</option> <option value="square">Retro (Square)</option> </select> </div> </div> </div> {/* MAIN WORKSPACE */} <div className="flex-1 flex flex-col overflow-hidden relative"> {/* 2. TIMELINE & MELODY EDITOR */} <div className="flex-1 flex flex-col relative bg-white"> <div className="h-8 bg-slate-100 border-b flex items-center px-4 text-xs font-semibold text-slate-500 shrink-0"> <div className="w-12 text-center border-r border-slate-300 pr-2">Takt</div> <div className="flex-1 flex justify-between ml-4"> <span>1</span><span>2</span><span>3</span><span>4</span> </div> </div> <div className="flex-1 relative flex"> {/* Y-Achsen Beschriftung */} <div className="w-12 bg-white/90 border-r flex flex-col justify-between text-[10px] text-slate-400 font-bold z-20 py-1 shrink-0"> {[...SOLFEGE].reverse().map((note, i) => ( <div key={${note}-${i}} className="flex-1 flex items-center justify-center border-b border-slate-100 w-full">{note}</div> ))} </div> {/* KLICKBARES RASTER */} <div className="flex-1 relative cursor-crosshair overflow-hidden" onClick={handleGridClick} style={{ backgroundImage: 'linear-gradient(to right, #f1f5f9 1px, transparent 1px), linear-gradient(to bottom, #f1f5f9 1px, transparent 1px)', backgroundSize: '6.25% 12.5%' }} > <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none shadow-[0_0_8px_rgba(239,68,68,0.6)]" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {melodyNotes.map(note => { const leftPercent = note.beat * 6.25; const topPercent = (8 - note.degree) * 12.5; // 8 Reihen const solfege = SOLFEGE[note.degree - 1]; return ( <div key={note.id} onClick={(e) => removeNote(e, note.id)} className="absolute bg-blue-400 rounded-sm border border-blue-600 shadow-sm flex items-center px-2 text-white text-xs font-bold hover:bg-red-400 hover:border-red-600 transition-colors cursor-pointer group z-10 overflow-hidden" style={{ left: ${leftPercent}%, top: ${topPercent}%, width: ${note.duration * 6.25}%, height: '12.5%' }} > <span className="truncate">{solfege}</span> <Trash2 size={10} className="ml-auto opacity-0 group-hover:opacity-100 shrink-0" /> </div> ); })} </div> </div> </div> {/* 3. CHORD BAND */} <div className="h-14 bg-slate-100 border-t border-b flex items-center shrink-0 overflow-x-hidden relative"> <div className="w-12 h-full border-r bg-slate-200 flex items-center justify-center shrink-0 z-20"> <Volume2 size={16} className="text-slate-500" /> </div> <div className="flex-1 relative h-full bg-white/40"> <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none opacity-50" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {chordSequence.length === 0 ? ( <div className="absolute inset-0 flex items-center px-4 text-sm text-slate-400 italic pointer-events-none"> Wähle eine Notenlänge und klicke unten auf einen Akkord... </div> ) : ( // NEU: Absolute Positionierung der Akkorde anhand ihrer Dauer! (() => { let currentLeftBeat = 0; return chordSequence.map((seqChord) => { const startBeat = currentLeftBeat; const visualDuration = Math.min(seqChord.duration, 16 - startBeat); currentLeftBeat += seqChord.duration; if (startBeat >= 16) return null; // Maximal 16 Beats anzeigen const currentChordData = diatonicChords.find(c => c.degree === seqChord.degree); const bgColors: Record<string, string> = { 'I': 'bg-red-100 border-red-400 text-red-700', 'ii': 'bg-orange-100 border-orange-400 text-orange-700', 'iii': 'bg-yellow-100 border-yellow-400 text-yellow-700', 'IV': 'bg-green-100 border-green-400 text-green-700', 'V': 'bg-blue-100 border-blue-400 text-blue-700', 'vi': 'bg-indigo-100 border-indigo-400 text-indigo-700', 'vii°': 'bg-purple-100 border-purple-400 text-purple-700', }; const leftPercent = (startBeat / 16) * 100; const widthPercent = (visualDuration / 16) * 100; return ( <div key={seqChord.id} className={absolute top-1 bottom-1 border-2 rounded-md flex items-center justify-center flex-col group cursor-pointer hover:opacity-80 transition-opacity ${bgColors[seqChord.degree]}} style={{ left: calc(${leftPercent}% + 2px), width: calc(${widthPercent}% - 4px) }} > <span className="font-bold leading-tight truncate w-full text-center px-1">{seqChord.degree}</span> {visualDuration >= 1 && <span className="text-[10px] font-medium opacity-80 truncate w-full text-center px-1">{currentChordData?.name}</span>} <button onClick={() => removeChord(seqChord.id)} className="absolute -top-2 -right-2 bg-slate-800 text-white rounded-full p-1 opacity-0 group-hover:opacity-100 transition-opacity z-10"><Trash2 size={12} /></button> </div> ); }); })() )} </div> </div> {/* --- PIANO VISUALIZER --- */} <div className="h-32 bg-slate-800 p-3 flex justify-center items-center shrink-0 border-t border-b border-slate-900 shadow-inner overflow-x-auto"> <div className="flex px-4"> {[3, 4, 5].map(oct => ( <React.Fragment key={oct}> <PianoWhiteKey note={C${oct}} hasBlack blackNote={Db${oct}} /> <PianoWhiteKey note={D${oct}} hasBlack blackNote={Eb${oct}} /> <PianoWhiteKey note={E${oct}} hasBlack={false} /> <PianoWhiteKey note={F${oct}} hasBlack blackNote={Gb${oct}} /> <PianoWhiteKey note={G${oct}} hasBlack blackNote={Ab${oct}} /> <PianoWhiteKey note={A${oct}} hasBlack blackNote={Bb${oct}} /> <PianoWhiteKey note={B${oct}} hasBlack={false} /> </React.Fragment> ))} <PianoWhiteKey note="C6" hasBlack={false} /> </div> </div> {/* 4. CHORD PALETTE */} <div className="h-28 bg-white p-4 shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.05)] shrink-0 flex flex-col gap-3 z-10"> <div className="flex gap-2 flex-1"> {diatonicChords.map((chord) => { const bgColors: Record<string, string> = { 'I': 'bg-red-50 hover:bg-red-100 border-red-200 text-red-700', 'ii': 'bg-orange-50 hover:bg-orange-100 border-orange-200 text-orange-700', 'iii': 'bg-yellow-50 hover:bg-yellow-100 border-yellow-200 text-yellow-700', 'IV': 'bg-green-50 hover:bg-green-100 border-green-200 text-green-700', 'V': 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700', 'vi': 'bg-indigo-50 hover:bg-indigo-100 border-indigo-200 text-indigo-700', 'vii°': 'bg-purple-50 hover:bg-purple-100 border-purple-200 text-purple-700', }; return ( <button key={chord.degree} onClick={() => playAndAddChord(chord.degree)} className={flex-1 flex flex-col items-center justify-center border-2 rounded-xl shadow-sm transition-all transform active:scale-95 ${bgColors[chord.degree]}} > <span className="font-extrabold text-2xl">{chord.degree}</span> <span className="text-sm font-medium opacity-75">{chord.name}</span> </button> ); })} </div> </div> </div> </div> ); }

// --- MUSIKTHEORIE-GEHIRN --- const NOTES = ['C', 'Db', 'D', 'Eb', 'E', 'F', 'Gb', 'G', 'Ab', 'A', 'Bb', 'B']; const MAJOR_INTERVALS = [0, 2, 4, 5, 7, 9, 11, 12]; const SOLFEGE = ['Do', 'Re', 'Mi', 'Fa', 'Sol', 'La', 'Ti', "Do'"]; const getNote = (rootName: string, interval: number, octave: number) => { const rootIndex = NOTES.indexOf(rootName); const totalSemitones = rootIndex + interval; const noteIndex = totalSemitones % 12; const octaveShift = Math.floor(totalSemitones / 12); return ${NOTES[noteIndex]}${octave + octaveShift}; }; export default function App() { const [isPlaying, setIsPlaying] = useState(false); const [currentRoot, setCurrentRoot] = useState("C"); // NEU: Akkorde speichern jetzt auch ihre Dauer (duration) in Beats! const [chordSequence, setChordSequence] = useState<{degree: string, id: string, duration: number}[]>([]); const [melodyNotes, setMelodyNotes] = useState<{id: string, beat: number, degree: number, duration: number}[]>([]); // States für Notenlänge und Instrumente const [selectedDuration, setSelectedDuration] = useState<number>(4); // Standard jetzt 4 (Ganze Note / ganzer Takt) const [melodyInstrument, setMelodyInstrument] = useState<Tone.ToneOscillatorType>("triangle"); const [chordInstrument, setChordInstrument] = useState<Tone.ToneOscillatorType>("sine"); const [showImportModal, setShowImportModal] = useState(false); const [importText, setImportText] = useState(""); const [isImporting, setIsImporting] = useState(false); const [playbackBeat, setPlaybackBeat] = useState(0); const animationRef = useRef<number>(); const [manualChordNotes, setManualChordNotes] = useState<string[]>([]); const [manualMelodyNotes, setManualMelodyNotes] = useState<string[]>([]); const manualChordTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const manualMelodyTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const chordSynthRef = useRef<Tone.PolySynth | null>(null); const melodySynthRef = useRef<Tone.PolySynth | null>(null); // Umgestellt auf PolySynth zur Fehlervermeidung bei überlappenden Noten const diatonicChords = useMemo(() => { return [ { degree: 'I', name: currentRoot, quality: 'M', notes: [getNote(currentRoot, 0, 4), getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4)] }, { degree: 'ii', name: getNote(currentRoot, 2, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 2, 4), getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4)] }, { degree: 'iii', name: getNote(currentRoot, 4, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4)] }, { degree: 'IV', name: getNote(currentRoot, 5, 0), quality: 'M', notes: [getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4)] }, { degree: 'V', name: getNote(currentRoot, 7, 0), quality: 'M', notes: [getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4)] }, { degree: 'vi', name: getNote(currentRoot, 9, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4), getNote(currentRoot, 16, 4)] }, { degree: 'vii°', name: getNote(currentRoot, 11, 0) + 'dim', quality: 'dim', notes: [getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4), getNote(currentRoot, 17, 4)] }, ]; }, [currentRoot]); // NEU: Berechnung der aktiven Akkord-Töne basierend auf variabler Akkordlänge const activeChordNotes = useMemo(() => { if (!isPlaying) return manualChordNotes; const notes = new Set<string>(); let currentBeatAccumulator = 0; for (const chordInfo of chordSequence) { if (playbackBeat >= currentBeatAccumulator && playbackBeat < currentBeatAccumulator + chordInfo.duration) { const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { chordData.notes.forEach(n => notes.add(n)); } break; // Wir haben den aktuell spielenden Akkord gefunden } currentBeatAccumulator += chordInfo.duration; } return Array.from(notes); }, [isPlaying, playbackBeat, chordSequence, diatonicChords, manualChordNotes]); const activeMelodyNotes = useMemo(() => { if (!isPlaying) return manualMelodyNotes; const notes = new Set<string>(); melodyNotes.forEach(note => { if (playbackBeat >= note.beat && playbackBeat < note.beat + note.duration) { const noteInterval = MAJOR_INTERVALS[note.degree - 1]; notes.add(getNote(currentRoot, noteInterval, 5)); } }); return Array.from(notes); }, [isPlaying, playbackBeat, melodyNotes, currentRoot, manualMelodyNotes]); const triggerManualChordNotes = (notes: string[], durationMs = 800) => { setManualChordNotes(notes); if (manualChordTimeoutRef.current) clearTimeout(manualChordTimeoutRef.current); manualChordTimeoutRef.current = setTimeout(() => setManualChordNotes([]), durationMs); }; const triggerManualMelodyNotes = (notes: string[], durationMs = 800) => { setManualMelodyNotes(notes); if (manualMelodyTimeoutRef.current) clearTimeout(manualMelodyTimeoutRef.current); manualMelodyTimeoutRef.current = setTimeout(() => setManualMelodyNotes([]), durationMs); }; const initAudio = async () => { if (Tone.context.state !== 'running') { await Tone.start(); } if (!chordSynthRef.current) { chordSynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); chordSynthRef.current.volume.value = -10; } if (!melodySynthRef.current) { // PolySynth verhindert Abstürze durch überlappende Noten zur selben Zählzeit melodySynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); melodySynthRef.current.volume.value = -6; } chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }; useEffect(() => { if (chordSynthRef.current) chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); if (melodySynthRef.current) melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }, [chordInstrument, melodyInstrument]); // NEU: Konvertierung von Beats in Tone.js Dauer (inklusive Ganzer Note '1m') const getToneDuration = (dur: number) => { if (dur === 0.25) return "16n"; if (dur === 0.5) return "8n"; if (dur === 1) return "4n"; if (dur === 2) return "2n"; if (dur === 4) return "1m"; // 1 ganzer Takt (measure) return "4n"; }; const togglePlayback = async () => { await initAudio(); if (isPlaying) { Tone.Transport.stop(); Tone.Transport.cancel(0); setIsPlaying(false); setPlaybackBeat(0); if (animationRef.current) cancelAnimationFrame(animationRef.current); } else { Tone.Transport.cancel(0); Tone.Transport.bpm.value = 120; Tone.Transport.loop = true; Tone.Transport.loopEnd = "4m"; // NEU: Akkorde anhand ihrer Dauer auf der Timeline platzieren let currentChordBeat = 0; chordSequence.forEach((chordInfo) => { if (currentChordBeat >= 16) return; // Maximal 4 Takte (16 Beats) const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { const measure = Math.floor(currentChordBeat / 4); const beat = Math.floor(currentChordBeat % 4); const sixteenths = Math.round((currentChordBeat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; Tone.Transport.schedule((time) => { chordSynthRef.current?.triggerAttackRelease(chordData.notes, getToneDuration(chordInfo.duration), time); }, startTime); } currentChordBeat += chordInfo.duration; }); // Melodie platzieren melodyNotes.forEach(note => { const measure = Math.floor(note.beat / 4); const beat = Math.floor(note.beat % 4); const sixteenths = Math.round((note.beat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; const noteInterval = MAJOR_INTERVALS[note.degree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); Tone.Transport.schedule((time) => { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(note.duration), time); }, startTime); }); Tone.Transport.start(); setIsPlaying(true); } }; useEffect(() => { const updateCursor = () => { if (isPlaying) { const currentTicks = Tone.Transport.ticks; const currentBeat = currentTicks / Tone.Transport.PPQ; setPlaybackBeat(currentBeat); animationRef.current = requestAnimationFrame(updateCursor); } }; if (isPlaying) { animationRef.current = requestAnimationFrame(updateCursor); } return () => { if (animationRef.current) cancelAnimationFrame(animationRef.current); }; }, [isPlaying]); useEffect(() => { if (isPlaying) togglePlayback(); // eslint-disable-next-line react-hooks/exhaustive-deps }, [currentRoot]); // NEU: Akkord fügt sich mit der aktuell gewählten Länge (selectedDuration) ein const playAndAddChord = async (chordDegree: string) => { await initAudio(); const chord = diatonicChords.find(c => c.degree === chordDegree); if (chord && chordSynthRef.current) { // Akkord mit der gewählten Länge abspielen (mindestens 8tel für gutes Feedback) const previewDuration = selectedDuration < 0.5 ? 0.5 : selectedDuration; chordSynthRef.current.triggerAttackRelease(chord.notes, getToneDuration(previewDuration)); setChordSequence([...chordSequence, { degree: chord.degree, id: Math.random().toString(), duration: selectedDuration }]); triggerManualChordNotes(chord.notes, previewDuration * 500); } }; const removeChord = (id: string) => setChordSequence(chordSequence.filter(c => c.id !== id)); const removeNote = (e: React.MouseEvent, id: string) => { e.stopPropagation(); setMelodyNotes(melodyNotes.filter(n => n.id !== id)); }; const handleGridClick = async (e: React.MouseEvent<HTMLDivElement>) => { const rect = e.currentTarget.getBoundingClientRect(); const clientX = e.clientX; const clientY = e.clientY; await initAudio(); const x = clientX - rect.left; const y = clientY - rect.top; const beatWidth = rect.width / 16; let clickedBeat = x / beatWidth; clickedBeat = Math.floor(clickedBeat / selectedDuration) * selectedDuration; const rowHeight = rect.height / 8; const clickedRowIndex = Math.floor(y / rowHeight); const scaleDegree = 8 - clickedRowIndex; const noteInterval = MAJOR_INTERVALS[scaleDegree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); if (!isPlaying) { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(selectedDuration)); triggerManualMelodyNotes([actualNote], selectedDuration * 500); } setMelodyNotes([...melodyNotes, { id: Math.random().toString(), beat: clickedBeat, degree: scaleDegree, duration: selectedDuration }]); }; const normalizeChordName = (rawChord: string) => { let clean = rawChord.replace(':maj', '').replace(':min', 'm'); clean = clean.charAt(0).toUpperCase() + clean.slice(1); return clean; }; const mapChordsToDegrees = (chordNamesArray: string[]) => { return chordNamesArray.map(chordName => { const match = diatonicChords.find(c => c.name.toLowerCase() === chordName.toLowerCase()); if (match) { // Standardmässig 4 Beats (1 ganzer Takt) pro importiertem Akkord return { degree: match.degree, id: Math.random().toString(), duration: 4 }; } return null; }).filter(Boolean) as {degree: string, id: string, duration: number}[]; }; const handleImport = async () => { const text = importText.trim(); if (!text) return; try { setIsImporting(true); if (text.startsWith('http')) { let html = ""; try { const proxyUrl = https://api.codetabs.com/v1/proxy?quest=${encodeURIComponent(text)}; const response = await fetch(proxyUrl); if (!response.ok) throw new Error("Proxy 1 failed"); html = await response.text(); } catch (err) { const proxyUrl2 = https://api.allorigins.win/raw?url=${encodeURIComponent(text)}; const response2 = await fetch(proxyUrl2); if (!response2.ok) throw new Error("Proxy 2 failed"); html = await response2.text(); } const chordRegex = /data-chord="([^"]+)"/g; let matches; const foundChords: string[] = []; while ((matches = chordRegex.exec(html)) !== null) { if (matches[1] !== "N") { foundChords.push(normalizeChordName(matches[1])); } } if (foundChords.length === 0) { alert("Konnte keine Akkorde auf dieser Seite finden. Bitte überprüfe die URL."); setIsImporting(false); return; } const uniqueProgression: string[] = []; for (const chord of foundChords) { if (uniqueProgression.length === 0 || uniqueProgression[uniqueProgression.length - 1] !== chord) { uniqueProgression.push(chord); } } const newChords = mapChordsToDegrees(uniqueProgression); if (newChords.length > 0) { setChordSequence(newChords); } else { alert(Akkorde gefunden, aber sie passen nicht zur aktuellen Tonart ${currentRoot}-Dur! Wechsle die Tonart und versuche es nochmals.); } } else if (text.startsWith('{')) { const data = JSON.parse(text); if (data.chords && Array.isArray(data.chords)) { const newChords = data.chords.map((c: any) => { // Unterstützt Strings oder Objekte mit duration if (typeof c === 'string') return { degree: c, id: Math.random().toString(), duration: 4 }; return { degree: c.degree, id: Math.random().toString(), duration: c.duration || 4 }; }); setChordSequence(newChords); } if (data.melody && Array.isArray(data.melody)) { const newMelody = data.melody.map((m: any) => ({ id: Math.random().toString(), beat: m.beat || 0, degree: m.degree || 1, duration: m.duration || 1 })); setMelodyNotes(newMelody); } } else { const chordNames = text.replace(/,/g, ' ').split(/\s+/).filter(Boolean).map(normalizeChordName); const newChords = mapChordsToDegrees(chordNames); if (newChords.length > 0) { setChordSequence(newChords); } } setShowImportModal(false); setImportText(""); } catch (error) { console.error(error); alert("Fehler beim Verarbeiten. Bitte überprüfe den Link oder das Format."); } finally { setIsImporting(false); } }; const displayMeasure = Math.floor(playbackBeat / 4) + 1; const displayBeat = Math.floor(playbackBeat % 4) + 1; const PianoWhiteKey = ({ note, hasBlack, blackNote }: { note: string, hasBlack: boolean, blackNote?: string }) => { const isWhiteMelody = activeMelodyNotes.includes(note); const isWhiteChord = activeChordNotes.includes(note); const isBlackMelody = blackNote ? activeMelodyNotes.includes(blackNote) : false; const isBlackChord = blackNote ? activeChordNotes.includes(blackNote) : false; let whiteBg = 'bg-white hover:bg-slate-50'; if (isWhiteMelody) whiteBg = 'bg-red-400 border-red-500 shadow-[inset_0_0_10px_rgba(239,68,68,0.5)] text-white'; else if (isWhiteChord) whiteBg = 'bg-blue-300 border-blue-400 shadow-[inset_0_0_10px_rgba(59,130,246,0.5)]'; let blackBg = 'bg-slate-900 shadow-[0_2px_2px_rgba(0,0,0,0.5)]'; if (isBlackMelody) blackBg = 'bg-red-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; else if (isBlackChord) blackBg = 'bg-blue-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; return ( <div className="relative flex-shrink-0 z-0"> <div className={w-8 h-24 border border-slate-300 rounded-b-md transition-colors duration-75 flex items-end justify-center pb-1 ${whiteBg}}> {note.startsWith('C') && <span className={text-[9px] font-bold ${isWhiteMelody ? 'text-red-100' : 'text-slate-400'}}>{note}</span>} </div> {hasBlack && ( <div className={absolute -right-3 top-0 w-6 h-14 rounded-b-sm z-10 transition-colors duration-75 ${blackBg}} /> )} </div> ); }; return ( <div className="h-screen w-full flex flex-col bg-slate-50 text-slate-800 font-sans select-none relative"> {/* IMPORT MODAL */} {showImportModal && ( <div className="absolute inset-0 bg-slate-900/40 backdrop-blur-sm z-50 flex items-center justify-center p-4"> <div className="bg-white rounded-xl shadow-2xl w-full max-w-lg overflow-hidden flex flex-col"> <div className="flex items-center justify-between p-4 border-b bg-slate-50"> <h2 className="font-bold text-lg text-slate-700">Song importieren</h2> <button onClick={() => setShowImportModal(false)} className="text-slate-400 hover:text-slate-600 disabled:opacity-50" disabled={isImporting}> <X size={20} /> </button> </div> <div className="p-6 flex flex-col gap-4"> <div> <label className="block text-sm font-bold text-slate-600 mb-1"> Chordify-Link, Text oder JSON </label> <textarea className="w-full h-32 border rounded-lg p-3 text-sm font-mono focus:ring-2 focus:ring-blue-500 focus:outline-none disabled:bg-slate-100 disabled:text-slate-400" placeholder="Füge hier eine URL ein (z.B. https://chordify.net/...)&#10;Oder tippe Akkorde: C G Am F" value={importText} onChange={(e) => setImportText(e.target.value)} disabled={isImporting} /> </div> </div> <div className="p-4 border-t bg-slate-50 flex justify-end gap-2"> <button onClick={() => setShowImportModal(false)} disabled={isImporting} className="px-4 py-2 rounded-lg font-semibold text-slate-600 hover:bg-slate-200 transition-colors disabled:opacity-50" > Abbrechen </button> <button onClick={handleImport} disabled={isImporting || !importText.trim()} className="flex items-center gap-2 px-4 py-2 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition-colors shadow-sm disabled:opacity-70" > {isImporting ? <><Loader2 size={16} className="animate-spin" />Analysiere...</> : "Importieren"} </button> </div> </div> </div> )} {/* 1. HEADER */} <header className="h-14 bg-white border-b flex items-center px-4 justify-between shadow-sm shrink-0 z-10"> <div className="flex items-center gap-6"> <h1 className="font-bold text-xl text-blue-600 tracking-tight">TheoryPad</h1> <div className="flex items-center gap-2 border-l pl-6"> <button onClick={togglePlayback} className={w-10 h-10 rounded-full flex items-center justify-center transition-colors shadow-sm text-white ${isPlaying ? 'bg-red-500 hover:bg-red-600' : 'bg-blue-500 hover:bg-blue-600'}} > {isPlaying ? <Square size={16} fill="currentColor" /> : <Play size={18} fill="currentColor" className="ml-1" />} </button> <div className="flex flex-col ml-2 min-w-[60px]"> <span className="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Position</span> <span className="text-sm font-semibold tabular-nums leading-none">Takt {displayMeasure}.{displayBeat}</span> </div> </div> </div> <div className="flex items-center gap-4"> <div className="flex bg-slate-100 rounded-md p-1 shadow-inner items-center"> <select className="bg-transparent px-2 py-1 font-semibold text-sm cursor-pointer hover:bg-slate-200 rounded outline-none appearance-none" value={currentRoot} onChange={(e) => setCurrentRoot(e.target.value)} > {NOTES.map(note => ( <option key={note} value={note}>{note} Major</option> ))} </select> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">120 BPM</div> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">4/4</div> </div> <button onClick={() => setShowImportModal(true)} className="flex items-center gap-1.5 px-3 py-1.5 bg-slate-800 text-white text-sm font-bold rounded-md hover:bg-slate-700 transition-colors shadow-sm" > <Download size={16} /> Import </button> </div> </header> {/* 1.5 TOOLBAR (Längen & Instrumente) */} <div className="h-12 bg-white border-b flex items-center px-4 justify-between shrink-0 text-sm z-10 shadow-sm"> <div className="flex items-center gap-2"> <Clock size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500 mr-2">Notenwert:</span> <div className="flex bg-slate-100 rounded-lg p-1"> {[ { label: '1/16', val: 0.25 }, { label: '1/8', val: 0.5 }, { label: '1/4', val: 1 }, { label: '1/2', val: 2 }, { label: '1/1 (Ganze)', val: 4 }, ].map(dur => ( <button key={dur.label} onClick={() => setSelectedDuration(dur.val)} className={px-3 py-1 rounded-md font-bold text-xs transition-colors ${selectedDuration === dur.val ? 'bg-white shadow-sm text-blue-600' : 'text-slate-500 hover:bg-slate-200'}} > {dur.label} </button> ))} </div> </div> <div className="flex items-center gap-6"> <div className="flex items-center gap-2"> <Music size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Melodie:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={melodyInstrument} onChange={(e) => setMelodyInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="triangle">Flöte (Triangle)</option> <option value="square">8-Bit (Square)</option> <option value="sawtooth">Synth Lead (Saw)</option> <option value="sine">Glocke (Sine)</option> </select> </div> <div className="flex items-center gap-2 border-l pl-6"> <Volume2 size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Akkorde:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={chordInstrument} onChange={(e) => setChordInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="sine">E-Piano (Sine)</option> <option value="triangle">Warm Pad (Triangle)</option> <option value="sawtooth">Trance (Saw)</option> <option value="square">Retro (Square)</option> </select> </div> </div> </div> {/* MAIN WORKSPACE */} <div className="flex-1 flex flex-col overflow-hidden relative"> {/* 2. TIMELINE & MELODY EDITOR */} <div className="flex-1 flex flex-col relative bg-white"> <div className="h-8 bg-slate-100 border-b flex items-center px-4 text-xs font-semibold text-slate-500 shrink-0"> <div className="w-12 text-center border-r border-slate-300 pr-2">Takt</div> <div className="flex-1 flex justify-between ml-4"> <span>1</span><span>2</span><span>3</span><span>4</span> </div> </div> <div className="flex-1 relative flex"> {/* Y-Achsen Beschriftung */} <div className="w-12 bg-white/90 border-r flex flex-col justify-between text-[10px] text-slate-400 font-bold z-20 py-1 shrink-0"> {[...SOLFEGE].reverse().map((note, i) => ( <div key={${note}-${i}} className="flex-1 flex items-center justify-center border-b border-slate-100 w-full">{note}</div> ))} </div> {/* KLICKBARES RASTER */} <div className="flex-1 relative cursor-crosshair overflow-hidden" onClick={handleGridClick} style={{ backgroundImage: 'linear-gradient(to right, #f1f5f9 1px, transparent 1px), linear-gradient(to bottom, #f1f5f9 1px, transparent 1px)', backgroundSize: '6.25% 12.5%' }} > <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none shadow-[0_0_8px_rgba(239,68,68,0.6)]" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {melodyNotes.map(note => { const leftPercent = note.beat * 6.25; const topPercent = (8 - note.degree) * 12.5; // 8 Reihen const solfege = SOLFEGE[note.degree - 1]; return ( <div key={note.id} onClick={(e) => removeNote(e, note.id)} className="absolute bg-blue-400 rounded-sm border border-blue-600 shadow-sm flex items-center px-2 text-white text-xs font-bold hover:bg-red-400 hover:border-red-600 transition-colors cursor-pointer group z-10 overflow-hidden" style={{ left: ${leftPercent}%, top: ${topPercent}%, width: ${note.duration * 6.25}%, height: '12.5%' }} > <span className="truncate">{solfege}</span> <Trash2 size={10} className="ml-auto opacity-0 group-hover:opacity-100 shrink-0" /> </div> ); })} </div> </div> </div> {/* 3. CHORD BAND */} <div className="h-14 bg-slate-100 border-t border-b flex items-center shrink-0 overflow-x-hidden relative"> <div className="w-12 h-full border-r bg-slate-200 flex items-center justify-center shrink-0 z-20"> <Volume2 size={16} className="text-slate-500" /> </div> <div className="flex-1 relative h-full bg-white/40"> <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none opacity-50" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {chordSequence.length === 0 ? ( <div className="absolute inset-0 flex items-center px-4 text-sm text-slate-400 italic pointer-events-none"> Wähle eine Notenlänge und klicke unten auf einen Akkord... </div> ) : ( // NEU: Absolute Positionierung der Akkorde anhand ihrer Dauer! (() => { let currentLeftBeat = 0; return chordSequence.map((seqChord) => { const startBeat = currentLeftBeat; const visualDuration = Math.min(seqChord.duration, 16 - startBeat); currentLeftBeat += seqChord.duration; if (startBeat >= 16) return null; // Maximal 16 Beats anzeigen const currentChordData = diatonicChords.find(c => c.degree === seqChord.degree); const bgColors: Record<string, string> = { 'I': 'bg-red-100 border-red-400 text-red-700', 'ii': 'bg-orange-100 border-orange-400 text-orange-700', 'iii': 'bg-yellow-100 border-yellow-400 text-yellow-700', 'IV': 'bg-green-100 border-green-400 text-green-700', 'V': 'bg-blue-100 border-blue-400 text-blue-700', 'vi': 'bg-indigo-100 border-indigo-400 text-indigo-700', 'vii°': 'bg-purple-100 border-purple-400 text-purple-700', }; const leftPercent = (startBeat / 16) * 100; const widthPercent = (visualDuration / 16) * 100; return ( <div key={seqChord.id} className={absolute top-1 bottom-1 border-2 rounded-md flex items-center justify-center flex-col group cursor-pointer hover:opacity-80 transition-opacity ${bgColors[seqChord.degree]}} style={{ left: calc(${leftPercent}% + 2px), width: calc(${widthPercent}% - 4px) }} > <span className="font-bold leading-tight truncate w-full text-center px-1">{seqChord.degree}</span> {visualDuration >= 1 && <span className="text-[10px] font-medium opacity-80 truncate w-full text-center px-1">{currentChordData?.name}</span>} <button onClick={() => removeChord(seqChord.id)} className="absolute -top-2 -right-2 bg-slate-800 text-white rounded-full p-1 opacity-0 group-hover:opacity-100 transition-opacity z-10"><Trash2 size={12} /></button> </div> ); }); })() )} </div> </div> {/* --- PIANO VISUALIZER --- */} <div className="h-32 bg-slate-800 p-3 flex justify-center items-center shrink-0 border-t border-b border-slate-900 shadow-inner overflow-x-auto"> <div className="flex px-4"> {[3, 4, 5].map(oct => ( <React.Fragment key={oct}> <PianoWhiteKey note={C${oct}} hasBlack blackNote={Db${oct}} /> <PianoWhiteKey note={D${oct}} hasBlack blackNote={Eb${oct}} /> <PianoWhiteKey note={E${oct}} hasBlack={false} /> <PianoWhiteKey note={F${oct}} hasBlack blackNote={Gb${oct}} /> <PianoWhiteKey note={G${oct}} hasBlack blackNote={Ab${oct}} /> <PianoWhiteKey note={A${oct}} hasBlack blackNote={Bb${oct}} /> <PianoWhiteKey note={B${oct}} hasBlack={false} /> </React.Fragment> ))} <PianoWhiteKey note="C6" hasBlack={false} /> </div> </div> {/* 4. CHORD PALETTE */} <div className="h-28 bg-white p-4 shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.05)] shrink-0 flex flex-col gap-3 z-10"> <div className="flex gap-2 flex-1"> {diatonicChords.map((chord) => { const bgColors: Record<string, string> = { 'I': 'bg-red-50 hover:bg-red-100 border-red-200 text-red-700', 'ii': 'bg-orange-50 hover:bg-orange-100 border-orange-200 text-orange-700', 'iii': 'bg-yellow-50 hover:bg-yellow-100 border-yellow-200 text-yellow-700', 'IV': 'bg-green-50 hover:bg-green-100 border-green-200 text-green-700', 'V': 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700', 'vi': 'bg-indigo-50 hover:bg-indigo-100 border-indigo-200 text-indigo-700', 'vii°': 'bg-purple-50 hover:bg-purple-100 border-purple-200 text-purple-700', }; return ( <button key={chord.degree} onClick={() => playAndAddChord(chord.degree)} className={flex-1 flex flex-col items-center justify-center border-2 rounded-xl shadow-sm transition-all transform active:scale-95 ${bgColors[chord.degree]}} > <span className="font-extrabold text-2xl">{chord.degree}</span> <span className="text-sm font-medium opacity-75">{chord.name}</span> </button> ); })} </div> </div> </div> </div> ); }// --- MUSIKTHEORIE-GEHIRN --- const NOTES = ['C', 'Db', 'D', 'Eb', 'E', 'F', 'Gb', 'G', 'Ab', 'A', 'Bb', 'B']; const MAJOR_INTERVALS = [0, 2, 4, 5, 7, 9, 11, 12]; const SOLFEGE = ['Do', 'Re', 'Mi', 'Fa', 'Sol', 'La', 'Ti', "Do'"]; const getNote = (rootName: string, interval: number, octave: number) => { const rootIndex = NOTES.indexOf(rootName); const totalSemitones = rootIndex + interval; const noteIndex = totalSemitones % 12; const octaveShift = Math.floor(totalSemitones / 12); return ${NOTES[noteIndex]}${octave + octaveShift}; }; export default function App() { const [isPlaying, setIsPlaying] = useState(false); const [currentRoot, setCurrentRoot] = useState("C"); // NEU: Akkorde speichern jetzt auch ihre Dauer (duration) in Beats! const [chordSequence, setChordSequence] = useState<{degree: string, id: string, duration: number}[]>([]); const [melodyNotes, setMelodyNotes] = useState<{id: string, beat: number, degree: number, duration: number}[]>([]); // States für Notenlänge und Instrumente const [selectedDuration, setSelectedDuration] = useState<number>(4); // Standard jetzt 4 (Ganze Note / ganzer Takt) const [melodyInstrument, setMelodyInstrument] = useState<Tone.ToneOscillatorType>("triangle"); const [chordInstrument, setChordInstrument] = useState<Tone.ToneOscillatorType>("sine"); const [showImportModal, setShowImportModal] = useState(false); const [importText, setImportText] = useState(""); const [isImporting, setIsImporting] = useState(false); const [playbackBeat, setPlaybackBeat] = useState(0); const animationRef = useRef<number>(); const [manualChordNotes, setManualChordNotes] = useState<string[]>([]); const [manualMelodyNotes, setManualMelodyNotes] = useState<string[]>([]); const manualChordTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const manualMelodyTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const chordSynthRef = useRef<Tone.PolySynth | null>(null); const melodySynthRef = useRef<Tone.PolySynth | null>(null); // Umgestellt auf PolySynth zur Fehlervermeidung bei überlappenden Noten const diatonicChords = useMemo(() => { return [ { degree: 'I', name: currentRoot, quality: 'M', notes: [getNote(currentRoot, 0, 4), getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4)] }, { degree: 'ii', name: getNote(currentRoot, 2, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 2, 4), getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4)] }, { degree: 'iii', name: getNote(currentRoot, 4, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4)] }, { degree: 'IV', name: getNote(currentRoot, 5, 0), quality: 'M', notes: [getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4)] }, { degree: 'V', name: getNote(currentRoot, 7, 0), quality: 'M', notes: [getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4)] }, { degree: 'vi', name: getNote(currentRoot, 9, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4), getNote(currentRoot, 16, 4)] }, { degree: 'vii°', name: getNote(currentRoot, 11, 0) + 'dim', quality: 'dim', notes: [getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4), getNote(currentRoot, 17, 4)] }, ]; }, [currentRoot]); // NEU: Berechnung der aktiven Akkord-Töne basierend auf variabler Akkordlänge const activeChordNotes = useMemo(() => { if (!isPlaying) return manualChordNotes; const notes = new Set<string>(); let currentBeatAccumulator = 0; for (const chordInfo of chordSequence) { if (playbackBeat >= currentBeatAccumulator && playbackBeat < currentBeatAccumulator + chordInfo.duration) { const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { chordData.notes.forEach(n => notes.add(n)); } break; // Wir haben den aktuell spielenden Akkord gefunden } currentBeatAccumulator += chordInfo.duration; } return Array.from(notes); }, [isPlaying, playbackBeat, chordSequence, diatonicChords, manualChordNotes]); const activeMelodyNotes = useMemo(() => { if (!isPlaying) return manualMelodyNotes; const notes = new Set<string>(); melodyNotes.forEach(note => { if (playbackBeat >= note.beat && playbackBeat < note.beat + note.duration) { const noteInterval = MAJOR_INTERVALS[note.degree - 1]; notes.add(getNote(currentRoot, noteInterval, 5)); } }); return Array.from(notes); }, [isPlaying, playbackBeat, melodyNotes, currentRoot, manualMelodyNotes]); const triggerManualChordNotes = (notes: string[], durationMs = 800) => { setManualChordNotes(notes); if (manualChordTimeoutRef.current) clearTimeout(manualChordTimeoutRef.current); manualChordTimeoutRef.current = setTimeout(() => setManualChordNotes([]), durationMs); }; const triggerManualMelodyNotes = (notes: string[], durationMs = 800) => { setManualMelodyNotes(notes); if (manualMelodyTimeoutRef.current) clearTimeout(manualMelodyTimeoutRef.current); manualMelodyTimeoutRef.current = setTimeout(() => setManualMelodyNotes([]), durationMs); }; const initAudio = async () => { if (Tone.context.state !== 'running') { await Tone.start(); } if (!chordSynthRef.current) { chordSynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); chordSynthRef.current.volume.value = -10; } if (!melodySynthRef.current) { // PolySynth verhindert Abstürze durch überlappende Noten zur selben Zählzeit melodySynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); melodySynthRef.current.volume.value = -6; } chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }; useEffect(() => { if (chordSynthRef.current) chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); if (melodySynthRef.current) melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }, [chordInstrument, melodyInstrument]); // NEU: Konvertierung von Beats in Tone.js Dauer (inklusive Ganzer Note '1m') const getToneDuration = (dur: number) => { if (dur === 0.25) return "16n"; if (dur === 0.5) return "8n"; if (dur === 1) return "4n"; if (dur === 2) return "2n"; if (dur === 4) return "1m"; // 1 ganzer Takt (measure) return "4n"; }; const togglePlayback = async () => { await initAudio(); if (isPlaying) { Tone.Transport.stop(); Tone.Transport.cancel(0); setIsPlaying(false); setPlaybackBeat(0); if (animationRef.current) cancelAnimationFrame(animationRef.current); } else { Tone.Transport.cancel(0); Tone.Transport.bpm.value = 120; Tone.Transport.loop = true; Tone.Transport.loopEnd = "4m"; // NEU: Akkorde anhand ihrer Dauer auf der Timeline platzieren let currentChordBeat = 0; chordSequence.forEach((chordInfo) => { if (currentChordBeat >= 16) return; // Maximal 4 Takte (16 Beats) const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { const measure = Math.floor(currentChordBeat / 4); const beat = Math.floor(currentChordBeat % 4); const sixteenths = Math.round((currentChordBeat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; Tone.Transport.schedule((time) => { chordSynthRef.current?.triggerAttackRelease(chordData.notes, getToneDuration(chordInfo.duration), time); }, startTime); } currentChordBeat += chordInfo.duration; }); // Melodie platzieren melodyNotes.forEach(note => { const measure = Math.floor(note.beat / 4); const beat = Math.floor(note.beat % 4); const sixteenths = Math.round((note.beat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; const noteInterval = MAJOR_INTERVALS[note.degree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); Tone.Transport.schedule((time) => { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(note.duration), time); }, startTime); }); Tone.Transport.start(); setIsPlaying(true); } }; useEffect(() => { const updateCursor = () => { if (isPlaying) { const currentTicks = Tone.Transport.ticks; const currentBeat = currentTicks / Tone.Transport.PPQ; setPlaybackBeat(currentBeat); animationRef.current = requestAnimationFrame(updateCursor); } }; if (isPlaying) { animationRef.current = requestAnimationFrame(updateCursor); } return () => { if (animationRef.current) cancelAnimationFrame(animationRef.current); }; }, [isPlaying]); useEffect(() => { if (isPlaying) togglePlayback(); // eslint-disable-next-line react-hooks/exhaustive-deps }, [currentRoot]); // NEU: Akkord fügt sich mit der aktuell gewählten Länge (selectedDuration) ein const playAndAddChord = async (chordDegree: string) => { await initAudio(); const chord = diatonicChords.find(c => c.degree === chordDegree); if (chord && chordSynthRef.current) { // Akkord mit der gewählten Länge abspielen (mindestens 8tel für gutes Feedback) const previewDuration = selectedDuration < 0.5 ? 0.5 : selectedDuration; chordSynthRef.current.triggerAttackRelease(chord.notes, getToneDuration(previewDuration)); setChordSequence([...chordSequence, { degree: chord.degree, id: Math.random().toString(), duration: selectedDuration }]); triggerManualChordNotes(chord.notes, previewDuration * 500); } }; const removeChord = (id: string) => setChordSequence(chordSequence.filter(c => c.id !== id)); const removeNote = (e: React.MouseEvent, id: string) => { e.stopPropagation(); setMelodyNotes(melodyNotes.filter(n => n.id !== id)); }; const handleGridClick = async (e: React.MouseEvent<HTMLDivElement>) => { const rect = e.currentTarget.getBoundingClientRect(); const clientX = e.clientX; const clientY = e.clientY; await initAudio(); const x = clientX - rect.left; const y = clientY - rect.top; const beatWidth = rect.width / 16; let clickedBeat = x / beatWidth; clickedBeat = Math.floor(clickedBeat / selectedDuration) * selectedDuration; const rowHeight = rect.height / 8; const clickedRowIndex = Math.floor(y / rowHeight); const scaleDegree = 8 - clickedRowIndex; const noteInterval = MAJOR_INTERVALS[scaleDegree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); if (!isPlaying) { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(selectedDuration)); triggerManualMelodyNotes([actualNote], selectedDuration * 500); } setMelodyNotes([...melodyNotes, { id: Math.random().toString(), beat: clickedBeat, degree: scaleDegree, duration: selectedDuration }]); }; const normalizeChordName = (rawChord: string) => { let clean = rawChord.replace(':maj', '').replace(':min', 'm'); clean = clean.charAt(0).toUpperCase() + clean.slice(1); return clean; }; const mapChordsToDegrees = (chordNamesArray: string[]) => { return chordNamesArray.map(chordName => { const match = diatonicChords.find(c => c.name.toLowerCase() === chordName.toLowerCase()); if (match) { // Standardmässig 4 Beats (1 ganzer Takt) pro importiertem Akkord return { degree: match.degree, id: Math.random().toString(), duration: 4 }; } return null; }).filter(Boolean) as {degree: string, id: string, duration: number}[]; }; const handleImport = async () => { const text = importText.trim(); if (!text) return; try { setIsImporting(true); if (text.startsWith('http')) { let html = ""; try { const proxyUrl = https://api.codetabs.com/v1/proxy?quest=${encodeURIComponent(text)}; const response = await fetch(proxyUrl); if (!response.ok) throw new Error("Proxy 1 failed"); html = await response.text(); } catch (err) { const proxyUrl2 = https://api.allorigins.win/raw?url=${encodeURIComponent(text)}; const response2 = await fetch(proxyUrl2); if (!response2.ok) throw new Error("Proxy 2 failed"); html = await response2.text(); } const chordRegex = /data-chord="([^"]+)"/g; let matches; const foundChords: string[] = []; while ((matches = chordRegex.exec(html)) !== null) { if (matches[1] !== "N") { foundChords.push(normalizeChordName(matches[1])); } } if (foundChords.length === 0) { alert("Konnte keine Akkorde auf dieser Seite finden. Bitte überprüfe die URL."); setIsImporting(false); return; } const uniqueProgression: string[] = []; for (const chord of foundChords) { if (uniqueProgression.length === 0 || uniqueProgression[uniqueProgression.length - 1] !== chord) { uniqueProgression.push(chord); } } const newChords = mapChordsToDegrees(uniqueProgression); if (newChords.length > 0) { setChordSequence(newChords); } else { alert(Akkorde gefunden, aber sie passen nicht zur aktuellen Tonart ${currentRoot}-Dur! Wechsle die Tonart und versuche es nochmals.); } } else if (text.startsWith('{')) { const data = JSON.parse(text); if (data.chords && Array.isArray(data.chords)) { const newChords = data.chords.map((c: any) => { // Unterstützt Strings oder Objekte mit duration if (typeof c === 'string') return { degree: c, id: Math.random().toString(), duration: 4 }; return { degree: c.degree, id: Math.random().toString(), duration: c.duration || 4 }; }); setChordSequence(newChords); } if (data.melody && Array.isArray(data.melody)) { const newMelody = data.melody.map((m: any) => ({ id: Math.random().toString(), beat: m.beat || 0, degree: m.degree || 1, duration: m.duration || 1 })); setMelodyNotes(newMelody); } } else { const chordNames = text.replace(/,/g, ' ').split(/\s+/).filter(Boolean).map(normalizeChordName); const newChords = mapChordsToDegrees(chordNames); if (newChords.length > 0) { setChordSequence(newChords); } } setShowImportModal(false); setImportText(""); } catch (error) { console.error(error); alert("Fehler beim Verarbeiten. Bitte überprüfe den Link oder das Format."); } finally { setIsImporting(false); } }; const displayMeasure = Math.floor(playbackBeat / 4) + 1; const displayBeat = Math.floor(playbackBeat % 4) + 1; const PianoWhiteKey = ({ note, hasBlack, blackNote }: { note: string, hasBlack: boolean, blackNote?: string }) => { const isWhiteMelody = activeMelodyNotes.includes(note); const isWhiteChord = activeChordNotes.includes(note); const isBlackMelody = blackNote ? activeMelodyNotes.includes(blackNote) : false; const isBlackChord = blackNote ? activeChordNotes.includes(blackNote) : false; let whiteBg = 'bg-white hover:bg-slate-50'; if (isWhiteMelody) whiteBg = 'bg-red-400 border-red-500 shadow-[inset_0_0_10px_rgba(239,68,68,0.5)] text-white'; else if (isWhiteChord) whiteBg = 'bg-blue-300 border-blue-400 shadow-[inset_0_0_10px_rgba(59,130,246,0.5)]'; let blackBg = 'bg-slate-900 shadow-[0_2px_2px_rgba(0,0,0,0.5)]'; if (isBlackMelody) blackBg = 'bg-red-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; else if (isBlackChord) blackBg = 'bg-blue-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; return ( <div className="relative flex-shrink-0 z-0"> <div className={w-8 h-24 border border-slate-300 rounded-b-md transition-colors duration-75 flex items-end justify-center pb-1 ${whiteBg}}> {note.startsWith('C') && <span className={text-[9px] font-bold ${isWhiteMelody ? 'text-red-100' : 'text-slate-400'}}>{note}</span>} </div> {hasBlack && ( <div className={absolute -right-3 top-0 w-6 h-14 rounded-b-sm z-10 transition-colors duration-75 ${blackBg}} /> )} </div> ); }; return ( <div className="h-screen w-full flex flex-col bg-slate-50 text-slate-800 font-sans select-none relative"> {/* IMPORT MODAL */} {showImportModal && ( <div className="absolute inset-0 bg-slate-900/40 backdrop-blur-sm z-50 flex items-center justify-center p-4"> <div className="bg-white rounded-xl shadow-2xl w-full max-w-lg overflow-hidden flex flex-col"> <div className="flex items-center justify-between p-4 border-b bg-slate-50"> <h2 className="font-bold text-lg text-slate-700">Song importieren</h2> <button onClick={() => setShowImportModal(false)} className="text-slate-400 hover:text-slate-600 disabled:opacity-50" disabled={isImporting}> <X size={20} /> </button> </div> <div className="p-6 flex flex-col gap-4"> <div> <label className="block text-sm font-bold text-slate-600 mb-1"> Chordify-Link, Text oder JSON </label> <textarea className="w-full h-32 border rounded-lg p-3 text-sm font-mono focus:ring-2 focus:ring-blue-500 focus:outline-none disabled:bg-slate-100 disabled:text-slate-400" placeholder="Füge hier eine URL ein (z.B. https://chordify.net/...)&#10;Oder tippe Akkorde: C G Am F" value={importText} onChange={(e) => setImportText(e.target.value)} disabled={isImporting} /> </div> </div> <div className="p-4 border-t bg-slate-50 flex justify-end gap-2"> <button onClick={() => setShowImportModal(false)} disabled={isImporting} className="px-4 py-2 rounded-lg font-semibold text-slate-600 hover:bg-slate-200 transition-colors disabled:opacity-50" > Abbrechen </button> <button onClick={handleImport} disabled={isImporting || !importText.trim()} className="flex items-center gap-2 px-4 py-2 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition-colors shadow-sm disabled:opacity-70" > {isImporting ? <><Loader2 size={16} className="animate-spin" />Analysiere...</> : "Importieren"} </button> </div> </div> </div> )} {/* 1. HEADER */} <header className="h-14 bg-white border-b flex items-center px-4 justify-between shadow-sm shrink-0 z-10"> <div className="flex items-center gap-6"> <h1 className="font-bold text-xl text-blue-600 tracking-tight">TheoryPad</h1> <div className="flex items-center gap-2 border-l pl-6"> <button onClick={togglePlayback} className={w-10 h-10 rounded-full flex items-center justify-center transition-colors shadow-sm text-white ${isPlaying ? 'bg-red-500 hover:bg-red-600' : 'bg-blue-500 hover:bg-blue-600'}} > {isPlaying ? <Square size={16} fill="currentColor" /> : <Play size={18} fill="currentColor" className="ml-1" />} </button> <div className="flex flex-col ml-2 min-w-[60px]"> <span className="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Position</span> <span className="text-sm font-semibold tabular-nums leading-none">Takt {displayMeasure}.{displayBeat}</span> </div> </div> </div> <div className="flex items-center gap-4"> <div className="flex bg-slate-100 rounded-md p-1 shadow-inner items-center"> <select className="bg-transparent px-2 py-1 font-semibold text-sm cursor-pointer hover:bg-slate-200 rounded outline-none appearance-none" value={currentRoot} onChange={(e) => setCurrentRoot(e.target.value)} > {NOTES.map(note => ( <option key={note} value={note}>{note} Major</option> ))} </select> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">120 BPM</div> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">4/4</div> </div> <button onClick={() => setShowImportModal(true)} className="flex items-center gap-1.5 px-3 py-1.5 bg-slate-800 text-white text-sm font-bold rounded-md hover:bg-slate-700 transition-colors shadow-sm" > <Download size={16} /> Import </button> </div> </header> {/* 1.5 TOOLBAR (Längen & Instrumente) */} <div className="h-12 bg-white border-b flex items-center px-4 justify-between shrink-0 text-sm z-10 shadow-sm"> <div className="flex items-center gap-2"> <Clock size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500 mr-2">Notenwert:</span> <div className="flex bg-slate-100 rounded-lg p-1"> {[ { label: '1/16', val: 0.25 }, { label: '1/8', val: 0.5 }, { label: '1/4', val: 1 }, { label: '1/2', val: 2 }, { label: '1/1 (Ganze)', val: 4 }, ].map(dur => ( <button key={dur.label} onClick={() => setSelectedDuration(dur.val)} className={px-3 py-1 rounded-md font-bold text-xs transition-colors ${selectedDuration === dur.val ? 'bg-white shadow-sm text-blue-600' : 'text-slate-500 hover:bg-slate-200'}} > {dur.label} </button> ))} </div> </div> <div className="flex items-center gap-6"> <div className="flex items-center gap-2"> <Music size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Melodie:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={melodyInstrument} onChange={(e) => setMelodyInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="triangle">Flöte (Triangle)</option> <option value="square">8-Bit (Square)</option> <option value="sawtooth">Synth Lead (Saw)</option> <option value="sine">Glocke (Sine)</option> </select> </div> <div className="flex items-center gap-2 border-l pl-6"> <Volume2 size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Akkorde:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={chordInstrument} onChange={(e) => setChordInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="sine">E-Piano (Sine)</option> <option value="triangle">Warm Pad (Triangle)</option> <option value="sawtooth">Trance (Saw)</option> <option value="square">Retro (Square)</option> </select> </div> </div> </div> {/* MAIN WORKSPACE */} <div className="flex-1 flex flex-col overflow-hidden relative"> {/* 2. TIMELINE & MELODY EDITOR */} <div className="flex-1 flex flex-col relative bg-white"> <div className="h-8 bg-slate-100 border-b flex items-center px-4 text-xs font-semibold text-slate-500 shrink-0"> <div className="w-12 text-center border-r border-slate-300 pr-2">Takt</div> <div className="flex-1 flex justify-between ml-4"> <span>1</span><span>2</span><span>3</span><span>4</span> </div> </div> <div className="flex-1 relative flex"> {/* Y-Achsen Beschriftung */} <div className="w-12 bg-white/90 border-r flex flex-col justify-between text-[10px] text-slate-400 font-bold z-20 py-1 shrink-0"> {[...SOLFEGE].reverse().map((note, i) => ( <div key={${note}-${i}} className="flex-1 flex items-center justify-center border-b border-slate-100 w-full">{note}</div> ))} </div> {/* KLICKBARES RASTER */} <div className="flex-1 relative cursor-crosshair overflow-hidden" onClick={handleGridClick} style={{ backgroundImage: 'linear-gradient(to right, #f1f5f9 1px, transparent 1px), linear-gradient(to bottom, #f1f5f9 1px, transparent 1px)', backgroundSize: '6.25% 12.5%' }} > <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none shadow-[0_0_8px_rgba(239,68,68,0.6)]" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {melodyNotes.map(note => { const leftPercent = note.beat * 6.25; const topPercent = (8 - note.degree) * 12.5; // 8 Reihen const solfege = SOLFEGE[note.degree - 1]; return ( <div key={note.id} onClick={(e) => removeNote(e, note.id)} className="absolute bg-blue-400 rounded-sm border border-blue-600 shadow-sm flex items-center px-2 text-white text-xs font-bold hover:bg-red-400 hover:border-red-600 transition-colors cursor-pointer group z-10 overflow-hidden" style={{ left: ${leftPercent}%, top: ${topPercent}%, width: ${note.duration * 6.25}%, height: '12.5%' }} > <span className="truncate">{solfege}</span> <Trash2 size={10} className="ml-auto opacity-0 group-hover:opacity-100 shrink-0" /> </div> ); })} </div> </div> </div> {/* 3. CHORD BAND */} <div className="h-14 bg-slate-100 border-t border-b flex items-center shrink-0 overflow-x-hidden relative"> <div className="w-12 h-full border-r bg-slate-200 flex items-center justify-center shrink-0 z-20"> <Volume2 size={16} className="text-slate-500" /> </div> <div className="flex-1 relative h-full bg-white/40"> <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none opacity-50" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {chordSequence.length === 0 ? ( <div className="absolute inset-0 flex items-center px-4 text-sm text-slate-400 italic pointer-events-none"> Wähle eine Notenlänge und klicke unten auf einen Akkord... </div> ) : ( // NEU: Absolute Positionierung der Akkorde anhand ihrer Dauer! (() => { let currentLeftBeat = 0; return chordSequence.map((seqChord) => { const startBeat = currentLeftBeat; const visualDuration = Math.min(seqChord.duration, 16 - startBeat); currentLeftBeat += seqChord.duration; if (startBeat >= 16) return null; // Maximal 16 Beats anzeigen const currentChordData = diatonicChords.find(c => c.degree === seqChord.degree); const bgColors: Record<string, string> = { 'I': 'bg-red-100 border-red-400 text-red-700', 'ii': 'bg-orange-100 border-orange-400 text-orange-700', 'iii': 'bg-yellow-100 border-yellow-400 text-yellow-700', 'IV': 'bg-green-100 border-green-400 text-green-700', 'V': 'bg-blue-100 border-blue-400 text-blue-700', 'vi': 'bg-indigo-100 border-indigo-400 text-indigo-700', 'vii°': 'bg-purple-100 border-purple-400 text-purple-700', }; const leftPercent = (startBeat / 16) * 100; const widthPercent = (visualDuration / 16) * 100; return ( <div key={seqChord.id} className={absolute top-1 bottom-1 border-2 rounded-md flex items-center justify-center flex-col group cursor-pointer hover:opacity-80 transition-opacity ${bgColors[seqChord.degree]}} style={{ left: calc(${leftPercent}% + 2px), width: calc(${widthPercent}% - 4px) }} > <span className="font-bold leading-tight truncate w-full text-center px-1">{seqChord.degree}</span> {visualDuration >= 1 && <span className="text-[10px] font-medium opacity-80 truncate w-full text-center px-1">{currentChordData?.name}</span>} <button onClick={() => removeChord(seqChord.id)} className="absolute -top-2 -right-2 bg-slate-800 text-white rounded-full p-1 opacity-0 group-hover:opacity-100 transition-opacity z-10"><Trash2 size={12} /></button> </div> ); }); })() )} </div> </div> {/* --- PIANO VISUALIZER --- */} <div className="h-32 bg-slate-800 p-3 flex justify-center items-center shrink-0 border-t border-b border-slate-900 shadow-inner overflow-x-auto"> <div className="flex px-4"> {[3, 4, 5].map(oct => ( <React.Fragment key={oct}> <PianoWhiteKey note={C${oct}} hasBlack blackNote={Db${oct}} /> <PianoWhiteKey note={D${oct}} hasBlack blackNote={Eb${oct}} /> <PianoWhiteKey note={E${oct}} hasBlack={false} /> <PianoWhiteKey note={F${oct}} hasBlack blackNote={Gb${oct}} /> <PianoWhiteKey note={G${oct}} hasBlack blackNote={Ab${oct}} /> <PianoWhiteKey note={A${oct}} hasBlack blackNote={Bb${oct}} /> <PianoWhiteKey note={B${oct}} hasBlack={false} /> </React.Fragment> ))} <PianoWhiteKey note="C6" hasBlack={false} /> </div> </div> {/* 4. CHORD PALETTE */} <div className="h-28 bg-white p-4 shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.05)] shrink-0 flex flex-col gap-3 z-10"> <div className="flex gap-2 flex-1"> {diatonicChords.map((chord) => { const bgColors: Record<string, string> = { 'I': 'bg-red-50 hover:bg-red-100 border-red-200 text-red-700', 'ii': 'bg-orange-50 hover:bg-orange-100 border-orange-200 text-orange-700', 'iii': 'bg-yellow-50 hover:bg-yellow-100 border-yellow-200 text-yellow-700', 'IV': 'bg-green-50 hover:bg-green-100 border-green-200 text-green-700', 'V': 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700', 'vi': 'bg-indigo-50 hover:bg-indigo-100 border-indigo-200 text-indigo-700', 'vii°': 'bg-purple-50 hover:bg-purple-100 border-purple-200 text-purple-700', }; return ( <button key={chord.degree} onClick={() => playAndAddChord(chord.degree)} className={flex-1 flex flex-col items-center justify-center border-2 rounded-xl shadow-sm transition-all transform active:scale-95 ${bgColors[chord.degree]}} > <span className="font-extrabold text-2xl">{chord.degree}</span> <span className="text-sm font-medium opacity-75">{chord.name}</span> </button> ); })} </div> </div> </div> </div> ); }// --- MUSIKTHEORIE-GEHIRN --- const NOTES = ['C', 'Db', 'D', 'Eb', 'E', 'F', 'Gb', 'G', 'Ab', 'A', 'Bb', 'B']; const MAJOR_INTERVALS = [0, 2, 4, 5, 7, 9, 11, 12]; const SOLFEGE = ['Do', 'Re', 'Mi', 'Fa', 'Sol', 'La', 'Ti', "Do'"]; const getNote = (rootName: string, interval: number, octave: number) => { const rootIndex = NOTES.indexOf(rootName); const totalSemitones = rootIndex + interval; const noteIndex = totalSemitones % 12; const octaveShift = Math.floor(totalSemitones / 12); return ${NOTES[noteIndex]}${octave + octaveShift}; }; export default function App() { const [isPlaying, setIsPlaying] = useState(false); const [currentRoot, setCurrentRoot] = useState("C"); // NEU: Akkorde speichern jetzt auch ihre Dauer (duration) in Beats! const [chordSequence, setChordSequence] = useState<{degree: string, id: string, duration: number}[]>([]); const [melodyNotes, setMelodyNotes] = useState<{id: string, beat: number, degree: number, duration: number}[]>([]); // States für Notenlänge und Instrumente const [selectedDuration, setSelectedDuration] = useState<number>(4); // Standard jetzt 4 (Ganze Note / ganzer Takt) const [melodyInstrument, setMelodyInstrument] = useState<Tone.ToneOscillatorType>("triangle"); const [chordInstrument, setChordInstrument] = useState<Tone.ToneOscillatorType>("sine"); const [showImportModal, setShowImportModal] = useState(false); const [importText, setImportText] = useState(""); const [isImporting, setIsImporting] = useState(false); const [playbackBeat, setPlaybackBeat] = useState(0); const animationRef = useRef<number>(); const [manualChordNotes, setManualChordNotes] = useState<string[]>([]); const [manualMelodyNotes, setManualMelodyNotes] = useState<string[]>([]); const manualChordTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const manualMelodyTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null); const chordSynthRef = useRef<Tone.PolySynth | null>(null); const melodySynthRef = useRef<Tone.PolySynth | null>(null); // Umgestellt auf PolySynth zur Fehlervermeidung bei überlappenden Noten const diatonicChords = useMemo(() => { return [ { degree: 'I', name: currentRoot, quality: 'M', notes: [getNote(currentRoot, 0, 4), getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4)] }, { degree: 'ii', name: getNote(currentRoot, 2, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 2, 4), getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4)] }, { degree: 'iii', name: getNote(currentRoot, 4, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 4, 4), getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4)] }, { degree: 'IV', name: getNote(currentRoot, 5, 0), quality: 'M', notes: [getNote(currentRoot, 5, 4), getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4)] }, { degree: 'V', name: getNote(currentRoot, 7, 0), quality: 'M', notes: [getNote(currentRoot, 7, 4), getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4)] }, { degree: 'vi', name: getNote(currentRoot, 9, 0) + 'm', quality: 'm', notes: [getNote(currentRoot, 9, 4), getNote(currentRoot, 12, 4), getNote(currentRoot, 16, 4)] }, { degree: 'vii°', name: getNote(currentRoot, 11, 0) + 'dim', quality: 'dim', notes: [getNote(currentRoot, 11, 4), getNote(currentRoot, 14, 4), getNote(currentRoot, 17, 4)] }, ]; }, [currentRoot]); // NEU: Berechnung der aktiven Akkord-Töne basierend auf variabler Akkordlänge const activeChordNotes = useMemo(() => { if (!isPlaying) return manualChordNotes; const notes = new Set<string>(); let currentBeatAccumulator = 0; for (const chordInfo of chordSequence) { if (playbackBeat >= currentBeatAccumulator && playbackBeat < currentBeatAccumulator + chordInfo.duration) { const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { chordData.notes.forEach(n => notes.add(n)); } break; // Wir haben den aktuell spielenden Akkord gefunden } currentBeatAccumulator += chordInfo.duration; } return Array.from(notes); }, [isPlaying, playbackBeat, chordSequence, diatonicChords, manualChordNotes]); const activeMelodyNotes = useMemo(() => { if (!isPlaying) return manualMelodyNotes; const notes = new Set<string>(); melodyNotes.forEach(note => { if (playbackBeat >= note.beat && playbackBeat < note.beat + note.duration) { const noteInterval = MAJOR_INTERVALS[note.degree - 1]; notes.add(getNote(currentRoot, noteInterval, 5)); } }); return Array.from(notes); }, [isPlaying, playbackBeat, melodyNotes, currentRoot, manualMelodyNotes]); const triggerManualChordNotes = (notes: string[], durationMs = 800) => { setManualChordNotes(notes); if (manualChordTimeoutRef.current) clearTimeout(manualChordTimeoutRef.current); manualChordTimeoutRef.current = setTimeout(() => setManualChordNotes([]), durationMs); }; const triggerManualMelodyNotes = (notes: string[], durationMs = 800) => { setManualMelodyNotes(notes); if (manualMelodyTimeoutRef.current) clearTimeout(manualMelodyTimeoutRef.current); manualMelodyTimeoutRef.current = setTimeout(() => setManualMelodyNotes([]), durationMs); }; const initAudio = async () => { if (Tone.context.state !== 'running') { await Tone.start(); } if (!chordSynthRef.current) { chordSynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); chordSynthRef.current.volume.value = -10; } if (!melodySynthRef.current) { // PolySynth verhindert Abstürze durch überlappende Noten zur selben Zählzeit melodySynthRef.current = new Tone.PolySynth(Tone.Synth).toDestination(); melodySynthRef.current.volume.value = -6; } chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }; useEffect(() => { if (chordSynthRef.current) chordSynthRef.current.set({ oscillator: { type: chordInstrument } }); if (melodySynthRef.current) melodySynthRef.current.set({ oscillator: { type: melodyInstrument } }); }, [chordInstrument, melodyInstrument]); // NEU: Konvertierung von Beats in Tone.js Dauer (inklusive Ganzer Note '1m') const getToneDuration = (dur: number) => { if (dur === 0.25) return "16n"; if (dur === 0.5) return "8n"; if (dur === 1) return "4n"; if (dur === 2) return "2n"; if (dur === 4) return "1m"; // 1 ganzer Takt (measure) return "4n"; }; const togglePlayback = async () => { await initAudio(); if (isPlaying) { Tone.Transport.stop(); Tone.Transport.cancel(0); setIsPlaying(false); setPlaybackBeat(0); if (animationRef.current) cancelAnimationFrame(animationRef.current); } else { Tone.Transport.cancel(0); Tone.Transport.bpm.value = 120; Tone.Transport.loop = true; Tone.Transport.loopEnd = "4m"; // NEU: Akkorde anhand ihrer Dauer auf der Timeline platzieren let currentChordBeat = 0; chordSequence.forEach((chordInfo) => { if (currentChordBeat >= 16) return; // Maximal 4 Takte (16 Beats) const chordData = diatonicChords.find(c => c.degree === chordInfo.degree); if (chordData) { const measure = Math.floor(currentChordBeat / 4); const beat = Math.floor(currentChordBeat % 4); const sixteenths = Math.round((currentChordBeat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; Tone.Transport.schedule((time) => { chordSynthRef.current?.triggerAttackRelease(chordData.notes, getToneDuration(chordInfo.duration), time); }, startTime); } currentChordBeat += chordInfo.duration; }); // Melodie platzieren melodyNotes.forEach(note => { const measure = Math.floor(note.beat / 4); const beat = Math.floor(note.beat % 4); const sixteenths = Math.round((note.beat % 1) * 4); const startTime = ${measure}:${beat}:${sixteenths}; const noteInterval = MAJOR_INTERVALS[note.degree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); Tone.Transport.schedule((time) => { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(note.duration), time); }, startTime); }); Tone.Transport.start(); setIsPlaying(true); } }; useEffect(() => { const updateCursor = () => { if (isPlaying) { const currentTicks = Tone.Transport.ticks; const currentBeat = currentTicks / Tone.Transport.PPQ; setPlaybackBeat(currentBeat); animationRef.current = requestAnimationFrame(updateCursor); } }; if (isPlaying) { animationRef.current = requestAnimationFrame(updateCursor); } return () => { if (animationRef.current) cancelAnimationFrame(animationRef.current); }; }, [isPlaying]); useEffect(() => { if (isPlaying) togglePlayback(); // eslint-disable-next-line react-hooks/exhaustive-deps }, [currentRoot]); // NEU: Akkord fügt sich mit der aktuell gewählten Länge (selectedDuration) ein const playAndAddChord = async (chordDegree: string) => { await initAudio(); const chord = diatonicChords.find(c => c.degree === chordDegree); if (chord && chordSynthRef.current) { // Akkord mit der gewählten Länge abspielen (mindestens 8tel für gutes Feedback) const previewDuration = selectedDuration < 0.5 ? 0.5 : selectedDuration; chordSynthRef.current.triggerAttackRelease(chord.notes, getToneDuration(previewDuration)); setChordSequence([...chordSequence, { degree: chord.degree, id: Math.random().toString(), duration: selectedDuration }]); triggerManualChordNotes(chord.notes, previewDuration * 500); } }; const removeChord = (id: string) => setChordSequence(chordSequence.filter(c => c.id !== id)); const removeNote = (e: React.MouseEvent, id: string) => { e.stopPropagation(); setMelodyNotes(melodyNotes.filter(n => n.id !== id)); }; const handleGridClick = async (e: React.MouseEvent<HTMLDivElement>) => { const rect = e.currentTarget.getBoundingClientRect(); const clientX = e.clientX; const clientY = e.clientY; await initAudio(); const x = clientX - rect.left; const y = clientY - rect.top; const beatWidth = rect.width / 16; let clickedBeat = x / beatWidth; clickedBeat = Math.floor(clickedBeat / selectedDuration) * selectedDuration; const rowHeight = rect.height / 8; const clickedRowIndex = Math.floor(y / rowHeight); const scaleDegree = 8 - clickedRowIndex; const noteInterval = MAJOR_INTERVALS[scaleDegree - 1]; const actualNote = getNote(currentRoot, noteInterval, 5); if (!isPlaying) { melodySynthRef.current?.triggerAttackRelease(actualNote, getToneDuration(selectedDuration)); triggerManualMelodyNotes([actualNote], selectedDuration * 500); } setMelodyNotes([...melodyNotes, { id: Math.random().toString(), beat: clickedBeat, degree: scaleDegree, duration: selectedDuration }]); }; const normalizeChordName = (rawChord: string) => { let clean = rawChord.replace(':maj', '').replace(':min', 'm'); clean = clean.charAt(0).toUpperCase() + clean.slice(1); return clean; }; const mapChordsToDegrees = (chordNamesArray: string[]) => { return chordNamesArray.map(chordName => { const match = diatonicChords.find(c => c.name.toLowerCase() === chordName.toLowerCase()); if (match) { // Standardmässig 4 Beats (1 ganzer Takt) pro importiertem Akkord return { degree: match.degree, id: Math.random().toString(), duration: 4 }; } return null; }).filter(Boolean) as {degree: string, id: string, duration: number}[]; }; const handleImport = async () => { const text = importText.trim(); if (!text) return; try { setIsImporting(true); if (text.startsWith('http')) { let html = ""; try { const proxyUrl = https://api.codetabs.com/v1/proxy?quest=${encodeURIComponent(text)}; const response = await fetch(proxyUrl); if (!response.ok) throw new Error("Proxy 1 failed"); html = await response.text(); } catch (err) { const proxyUrl2 = https://api.allorigins.win/raw?url=${encodeURIComponent(text)}; const response2 = await fetch(proxyUrl2); if (!response2.ok) throw new Error("Proxy 2 failed"); html = await response2.text(); } const chordRegex = /data-chord="([^"]+)"/g; let matches; const foundChords: string[] = []; while ((matches = chordRegex.exec(html)) !== null) { if (matches[1] !== "N") { foundChords.push(normalizeChordName(matches[1])); } } if (foundChords.length === 0) { alert("Konnte keine Akkorde auf dieser Seite finden. Bitte überprüfe die URL."); setIsImporting(false); return; } const uniqueProgression: string[] = []; for (const chord of foundChords) { if (uniqueProgression.length === 0 || uniqueProgression[uniqueProgression.length - 1] !== chord) { uniqueProgression.push(chord); } } const newChords = mapChordsToDegrees(uniqueProgression); if (newChords.length > 0) { setChordSequence(newChords); } else { alert(Akkorde gefunden, aber sie passen nicht zur aktuellen Tonart ${currentRoot}-Dur! Wechsle die Tonart und versuche es nochmals.); } } else if (text.startsWith('{')) { const data = JSON.parse(text); if (data.chords && Array.isArray(data.chords)) { const newChords = data.chords.map((c: any) => { // Unterstützt Strings oder Objekte mit duration if (typeof c === 'string') return { degree: c, id: Math.random().toString(), duration: 4 }; return { degree: c.degree, id: Math.random().toString(), duration: c.duration || 4 }; }); setChordSequence(newChords); } if (data.melody && Array.isArray(data.melody)) { const newMelody = data.melody.map((m: any) => ({ id: Math.random().toString(), beat: m.beat || 0, degree: m.degree || 1, duration: m.duration || 1 })); setMelodyNotes(newMelody); } } else { const chordNames = text.replace(/,/g, ' ').split(/\s+/).filter(Boolean).map(normalizeChordName); const newChords = mapChordsToDegrees(chordNames); if (newChords.length > 0) { setChordSequence(newChords); } } setShowImportModal(false); setImportText(""); } catch (error) { console.error(error); alert("Fehler beim Verarbeiten. Bitte überprüfe den Link oder das Format."); } finally { setIsImporting(false); } }; const displayMeasure = Math.floor(playbackBeat / 4) + 1; const displayBeat = Math.floor(playbackBeat % 4) + 1; const PianoWhiteKey = ({ note, hasBlack, blackNote }: { note: string, hasBlack: boolean, blackNote?: string }) => { const isWhiteMelody = activeMelodyNotes.includes(note); const isWhiteChord = activeChordNotes.includes(note); const isBlackMelody = blackNote ? activeMelodyNotes.includes(blackNote) : false; const isBlackChord = blackNote ? activeChordNotes.includes(blackNote) : false; let whiteBg = 'bg-white hover:bg-slate-50'; if (isWhiteMelody) whiteBg = 'bg-red-400 border-red-500 shadow-[inset_0_0_10px_rgba(239,68,68,0.5)] text-white'; else if (isWhiteChord) whiteBg = 'bg-blue-300 border-blue-400 shadow-[inset_0_0_10px_rgba(59,130,246,0.5)]'; let blackBg = 'bg-slate-900 shadow-[0_2px_2px_rgba(0,0,0,0.5)]'; if (isBlackMelody) blackBg = 'bg-red-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; else if (isBlackChord) blackBg = 'bg-blue-500 shadow-[0_2px_4px_rgba(0,0,0,0.4)]'; return ( <div className="relative flex-shrink-0 z-0"> <div className={w-8 h-24 border border-slate-300 rounded-b-md transition-colors duration-75 flex items-end justify-center pb-1 ${whiteBg}}> {note.startsWith('C') && <span className={text-[9px] font-bold ${isWhiteMelody ? 'text-red-100' : 'text-slate-400'}}>{note}</span>} </div> {hasBlack && ( <div className={absolute -right-3 top-0 w-6 h-14 rounded-b-sm z-10 transition-colors duration-75 ${blackBg}} /> )} </div> ); }; return ( <div className="h-screen w-full flex flex-col bg-slate-50 text-slate-800 font-sans select-none relative"> {/* IMPORT MODAL */} {showImportModal && ( <div className="absolute inset-0 bg-slate-900/40 backdrop-blur-sm z-50 flex items-center justify-center p-4"> <div className="bg-white rounded-xl shadow-2xl w-full max-w-lg overflow-hidden flex flex-col"> <div className="flex items-center justify-between p-4 border-b bg-slate-50"> <h2 className="font-bold text-lg text-slate-700">Song importieren</h2> <button onClick={() => setShowImportModal(false)} className="text-slate-400 hover:text-slate-600 disabled:opacity-50" disabled={isImporting}> <X size={20} /> </button> </div> <div className="p-6 flex flex-col gap-4"> <div> <label className="block text-sm font-bold text-slate-600 mb-1"> Chordify-Link, Text oder JSON </label> <textarea className="w-full h-32 border rounded-lg p-3 text-sm font-mono focus:ring-2 focus:ring-blue-500 focus:outline-none disabled:bg-slate-100 disabled:text-slate-400" placeholder="Füge hier eine URL ein (z.B. https://chordify.net/...)&#10;Oder tippe Akkorde: C G Am F" value={importText} onChange={(e) => setImportText(e.target.value)} disabled={isImporting} /> </div> </div> <div className="p-4 border-t bg-slate-50 flex justify-end gap-2"> <button onClick={() => setShowImportModal(false)} disabled={isImporting} className="px-4 py-2 rounded-lg font-semibold text-slate-600 hover:bg-slate-200 transition-colors disabled:opacity-50" > Abbrechen </button> <button onClick={handleImport} disabled={isImporting || !importText.trim()} className="flex items-center gap-2 px-4 py-2 rounded-lg font-semibold bg-blue-600 text-white hover:bg-blue-700 transition-colors shadow-sm disabled:opacity-70" > {isImporting ? <><Loader2 size={16} className="animate-spin" />Analysiere...</> : "Importieren"} </button> </div> </div> </div> )} {/* 1. HEADER */} <header className="h-14 bg-white border-b flex items-center px-4 justify-between shadow-sm shrink-0 z-10"> <div className="flex items-center gap-6"> <h1 className="font-bold text-xl text-blue-600 tracking-tight">TheoryPad</h1> <div className="flex items-center gap-2 border-l pl-6"> <button onClick={togglePlayback} className={w-10 h-10 rounded-full flex items-center justify-center transition-colors shadow-sm text-white ${isPlaying ? 'bg-red-500 hover:bg-red-600' : 'bg-blue-500 hover:bg-blue-600'}} > {isPlaying ? <Square size={16} fill="currentColor" /> : <Play size={18} fill="currentColor" className="ml-1" />} </button> <div className="flex flex-col ml-2 min-w-[60px]"> <span className="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Position</span> <span className="text-sm font-semibold tabular-nums leading-none">Takt {displayMeasure}.{displayBeat}</span> </div> </div> </div> <div className="flex items-center gap-4"> <div className="flex bg-slate-100 rounded-md p-1 shadow-inner items-center"> <select className="bg-transparent px-2 py-1 font-semibold text-sm cursor-pointer hover:bg-slate-200 rounded outline-none appearance-none" value={currentRoot} onChange={(e) => setCurrentRoot(e.target.value)} > {NOTES.map(note => ( <option key={note} value={note}>{note} Major</option> ))} </select> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">120 BPM</div> <div className="px-3 py-1 font-semibold text-sm border-l border-slate-300">4/4</div> </div> <button onClick={() => setShowImportModal(true)} className="flex items-center gap-1.5 px-3 py-1.5 bg-slate-800 text-white text-sm font-bold rounded-md hover:bg-slate-700 transition-colors shadow-sm" > <Download size={16} /> Import </button> </div> </header> {/* 1.5 TOOLBAR (Längen & Instrumente) */} <div className="h-12 bg-white border-b flex items-center px-4 justify-between shrink-0 text-sm z-10 shadow-sm"> <div className="flex items-center gap-2"> <Clock size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500 mr-2">Notenwert:</span> <div className="flex bg-slate-100 rounded-lg p-1"> {[ { label: '1/16', val: 0.25 }, { label: '1/8', val: 0.5 }, { label: '1/4', val: 1 }, { label: '1/2', val: 2 }, { label: '1/1 (Ganze)', val: 4 }, ].map(dur => ( <button key={dur.label} onClick={() => setSelectedDuration(dur.val)} className={px-3 py-1 rounded-md font-bold text-xs transition-colors ${selectedDuration === dur.val ? 'bg-white shadow-sm text-blue-600' : 'text-slate-500 hover:bg-slate-200'}} > {dur.label} </button> ))} </div> </div> <div className="flex items-center gap-6"> <div className="flex items-center gap-2"> <Music size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Melodie:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={melodyInstrument} onChange={(e) => setMelodyInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="triangle">Flöte (Triangle)</option> <option value="square">8-Bit (Square)</option> <option value="sawtooth">Synth Lead (Saw)</option> <option value="sine">Glocke (Sine)</option> </select> </div> <div className="flex items-center gap-2 border-l pl-6"> <Volume2 size={16} className="text-slate-400" /> <span className="font-semibold text-slate-500">Akkorde:</span> <select className="bg-slate-100 px-2 py-1 font-semibold text-xs rounded outline-none border hover:border-slate-300" value={chordInstrument} onChange={(e) => setChordInstrument(e.target.value as Tone.ToneOscillatorType)} > <option value="sine">E-Piano (Sine)</option> <option value="triangle">Warm Pad (Triangle)</option> <option value="sawtooth">Trance (Saw)</option> <option value="square">Retro (Square)</option> </select> </div> </div> </div> {/* MAIN WORKSPACE */} <div className="flex-1 flex flex-col overflow-hidden relative"> {/* 2. TIMELINE & MELODY EDITOR */} <div className="flex-1 flex flex-col relative bg-white"> <div className="h-8 bg-slate-100 border-b flex items-center px-4 text-xs font-semibold text-slate-500 shrink-0"> <div className="w-12 text-center border-r border-slate-300 pr-2">Takt</div> <div className="flex-1 flex justify-between ml-4"> <span>1</span><span>2</span><span>3</span><span>4</span> </div> </div> <div className="flex-1 relative flex"> {/* Y-Achsen Beschriftung */} <div className="w-12 bg-white/90 border-r flex flex-col justify-between text-[10px] text-slate-400 font-bold z-20 py-1 shrink-0"> {[...SOLFEGE].reverse().map((note, i) => ( <div key={${note}-${i}} className="flex-1 flex items-center justify-center border-b border-slate-100 w-full">{note}</div> ))} </div> {/* KLICKBARES RASTER */} <div className="flex-1 relative cursor-crosshair overflow-hidden" onClick={handleGridClick} style={{ backgroundImage: 'linear-gradient(to right, #f1f5f9 1px, transparent 1px), linear-gradient(to bottom, #f1f5f9 1px, transparent 1px)', backgroundSize: '6.25% 12.5%' }} > <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none shadow-[0_0_8px_rgba(239,68,68,0.6)]" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {melodyNotes.map(note => { const leftPercent = note.beat * 6.25; const topPercent = (8 - note.degree) * 12.5; // 8 Reihen const solfege = SOLFEGE[note.degree - 1]; return ( <div key={note.id} onClick={(e) => removeNote(e, note.id)} className="absolute bg-blue-400 rounded-sm border border-blue-600 shadow-sm flex items-center px-2 text-white text-xs font-bold hover:bg-red-400 hover:border-red-600 transition-colors cursor-pointer group z-10 overflow-hidden" style={{ left: ${leftPercent}%, top: ${topPercent}%, width: ${note.duration * 6.25}%, height: '12.5%' }} > <span className="truncate">{solfege}</span> <Trash2 size={10} className="ml-auto opacity-0 group-hover:opacity-100 shrink-0" /> </div> ); })} </div> </div> </div> {/* 3. CHORD BAND */} <div className="h-14 bg-slate-100 border-t border-b flex items-center shrink-0 overflow-x-hidden relative"> <div className="w-12 h-full border-r bg-slate-200 flex items-center justify-center shrink-0 z-20"> <Volume2 size={16} className="text-slate-500" /> </div> <div className="flex-1 relative h-full bg-white/40"> <div className="absolute top-0 bottom-0 w-[2px] bg-red-500 z-30 pointer-events-none transition-none opacity-50" style={{ left: ${(playbackBeat / 16) * 100}%, display: isPlaying || playbackBeat > 0 ? 'block' : 'none' }} /> {chordSequence.length === 0 ? ( <div className="absolute inset-0 flex items-center px-4 text-sm text-slate-400 italic pointer-events-none"> Wähle eine Notenlänge und klicke unten auf einen Akkord... </div> ) : ( // NEU: Absolute Positionierung der Akkorde anhand ihrer Dauer! (() => { let currentLeftBeat = 0; return chordSequence.map((seqChord) => { const startBeat = currentLeftBeat; const visualDuration = Math.min(seqChord.duration, 16 - startBeat); currentLeftBeat += seqChord.duration; if (startBeat >= 16) return null; // Maximal 16 Beats anzeigen const currentChordData = diatonicChords.find(c => c.degree === seqChord.degree); const bgColors: Record<string, string> = { 'I': 'bg-red-100 border-red-400 text-red-700', 'ii': 'bg-orange-100 border-orange-400 text-orange-700', 'iii': 'bg-yellow-100 border-yellow-400 text-yellow-700', 'IV': 'bg-green-100 border-green-400 text-green-700', 'V': 'bg-blue-100 border-blue-400 text-blue-700', 'vi': 'bg-indigo-100 border-indigo-400 text-indigo-700', 'vii°': 'bg-purple-100 border-purple-400 text-purple-700', }; const leftPercent = (startBeat / 16) * 100; const widthPercent = (visualDuration / 16) * 100; return ( <div key={seqChord.id} className={absolute top-1 bottom-1 border-2 rounded-md flex items-center justify-center flex-col group cursor-pointer hover:opacity-80 transition-opacity ${bgColors[seqChord.degree]}} style={{ left: calc(${leftPercent}% + 2px), width: calc(${widthPercent}% - 4px) }} > <span className="font-bold leading-tight truncate w-full text-center px-1">{seqChord.degree}</span> {visualDuration >= 1 && <span className="text-[10px] font-medium opacity-80 truncate w-full text-center px-1">{currentChordData?.name}</span>} <button onClick={() => removeChord(seqChord.id)} className="absolute -top-2 -right-2 bg-slate-800 text-white rounded-full p-1 opacity-0 group-hover:opacity-100 transition-opacity z-10"><Trash2 size={12} /></button> </div> ); }); })() )} </div> </div> {/* --- PIANO VISUALIZER --- */} <div className="h-32 bg-slate-800 p-3 flex justify-center items-center shrink-0 border-t border-b border-slate-900 shadow-inner overflow-x-auto"> <div className="flex px-4"> {[3, 4, 5].map(oct => ( <React.Fragment key={oct}> <PianoWhiteKey note={C${oct}} hasBlack blackNote={Db${oct}} /> <PianoWhiteKey note={D${oct}} hasBlack blackNote={Eb${oct}} /> <PianoWhiteKey note={E${oct}} hasBlack={false} /> <PianoWhiteKey note={F${oct}} hasBlack blackNote={Gb${oct}} /> <PianoWhiteKey note={G${oct}} hasBlack blackNote={Ab${oct}} /> <PianoWhiteKey note={A${oct}} hasBlack blackNote={Bb${oct}} /> <PianoWhiteKey note={B${oct}} hasBlack={false} /> </React.Fragment> ))} <PianoWhiteKey note="C6" hasBlack={false} /> </div> </div> {/* 4. CHORD PALETTE */} <div className="h-28 bg-white p-4 shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.05)] shrink-0 flex flex-col gap-3 z-10"> <div className="flex gap-2 flex-1"> {diatonicChords.map((chord) => { const bgColors: Record<string, string> = { 'I': 'bg-red-50 hover:bg-red-100 border-red-200 text-red-700', 'ii': 'bg-orange-50 hover:bg-orange-100 border-orange-200 text-orange-700', 'iii': 'bg-yellow-50 hover:bg-yellow-100 border-yellow-200 text-yellow-700', 'IV': 'bg-green-50 hover:bg-green-100 border-green-200 text-green-700', 'V': 'bg-blue-50 hover:bg-blue-100 border-blue-200 text-blue-700', 'vi': 'bg-indigo-50 hover:bg-indigo-100 border-indigo-200 text-indigo-700', 'vii°': 'bg-purple-50 hover:bg-purple-100 border-purple-200 text-purple-700', }; return ( <button key={chord.degree} onClick={() => playAndAddChord(chord.degree)} className={flex-1 flex flex-col items-center justify-center border-2 rounded-xl shadow-sm transition-all transform active:scale-95 ${bgColors[chord.degree]}} > <span className="font-extrabold text-2xl">{chord.degree}</span> <span className="text-sm font-medium opacity-75">{chord.name}</span> </button> ); })} </div> </div> </div> </div> ); }
 
